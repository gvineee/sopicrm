# Codex ↔ Claude Code handoff

This file is the shared coordination point for the two coding sessions working
in the Sopi CRM workspace. Read it before editing and update the handoff after
each bounded task. The repository is the source of truth; chat messages are not
a substitute for checking the current worktree.

## Ownership

- **Codex:** domain architecture, migrations, Laravel actions/services/jobs,
  policies, APIs, integration contracts, database/RLS, automated tests and
  release gates.
- **Claude Code:** Vue/Inertia UI, responsive UX, frontend components and
  frontend tests, unless a task is explicitly reassigned here.
- **Shared files:** `routes/modules/*`, module navigation config,
  `resources/js/lib/navIcons.ts`, and documentation. Touch these only after
  checking `git diff` and recording the intended change below.

## No-interference protocol

1. Run `git status --short` and inspect the relevant diff before editing.
2. One agent owns a file at a time. Do not rewrite, reset, or delete another
   agent's uncommitted work.
3. Keep changes additive and preserve existing public contracts. If a method or
   prop must change, record the old and new shape in this file first.
4. Run the narrowest relevant tests, then the full gates before handing off:
   PHPStan, Pint, `php artisan test`, `npm run types:check`, and `npm run build`.
5. Do not commit, push, add deploy keys, or change external permissions from an
   agent session without an explicit user request.

## Current handoff

### 2026-09-22 — Claude Code, overnight execution: Assets QR-scan + Maintenance scheduling (closes REQ-AST-02/07)

- **Scope:** the two remaining real Assets gaps the `implementation-plan.md` status audit correctly left `in-progress` rather than rubber-stamping: QR-scan resolution and Maintenance vendor/due-date/cost scheduling.
- **QR-scan (REQ-AST-02):** new `GET /assets/qr/{token}` re-runs the real `AssetPolicy::view` check before redirecting to the asset's Show page — an unknown token and a token for an asset the caller cannot view both 404 identically, never a 403 (which would itself confirm the token's validity to an unauthorized prober). No in-app QR-decoding library was added: a physical asset's printed QR code encodes this route's own full URL directly, so "scanning" is a plain camera-app QR-to-URL open on any modern phone, matching this codebase's established preference for building a primitive directly over adding a dependency.
- **Maintenance (REQ-AST-07):** new `ScheduleMaintenanceAction` (creates a record only — does not itself flip `Asset.condition`, since a routine/preventive booking for a `good` asset shouldn't be force-marked `under_repair`) and `CompleteMaintenanceAction` (sets `completed_at`/`actual_cost`/`next_service_due_at`, and restores `condition` to `good` ONLY if the asset is currently `under_repair` — never downgrades an unrelated `written_off` asset). New `MaintenancePolicy` reuses `assets.assets.manage` (no new permission). Real UI on `Assets/Show.vue`.
- **Real pre-existing bug found and fixed:** the `Maintenance` model had no explicit `$table` property, so Eloquent guessed the plural `maintenances` — but the real migration (`2026_09_16_090190_create_assets_domain_tables.php`) created a SINGULAR `maintenance` table. This table had zero rows/queries against it anywhere in the codebase until this pass wrote the first real Actions that actually touch it, so the mismatch was previously completely latent (never caught by any test, since nothing queried it). Fixed with an explicit `protected $table = 'maintenance'`.
- **Files changed:** new `app/Domain/Assets/Actions/{ScheduleMaintenanceAction,CompleteMaintenanceAction}.php`, new `app/Policies/MaintenancePolicy.php`, new `app/Http/Controllers/Assets/MaintenanceController.php`, new `app/Http/Requests/Assets/{ScheduleMaintenanceRequest,CompleteMaintenanceRequest}.php`, new `app/Http/Resources/Assets/MaintenanceResource.php`; `app/Http/Controllers/Assets/AssetController.php` (+`scanQr()`, Show now loads/passes `maintenanceRecords`), `app/Domain/Assets/Models/Maintenance.php` (table-name fix + `@property` docblocks), `app/Providers/Assets/AssetsModuleServiceProvider.php` (+policy registration), `routes/modules/web-assets.php` (+3 routes), `resources/js/pages/Assets/Show.vue` (real maintenance section); new `tests/Feature/Assets/{QrScanResolutionTest,MaintenanceSchedulingTest}.php` (9 tests); `docs/implementation-plan.md` (REQ-AST-02/07 → `done`).
- **Contract/schema changes:** none — both tables already existed.
- **Tests/gates:** `php artisan test --compact` → **295 passed / 3 skipped** (1441 assertions). `phpstan analyse` → 0 errors. `pint --dirty` → clean. `npm run types:check`/`build` → passed. One transient, unrelated failure (`ProfileUpdateTest`, a stale Vite font manifest read caused by running `npm run build` concurrently with an in-flight test run) — confirmed not a real bug by rerunning that test alone (passed) and the whole suite once more sequentially (clean).
- **Next:** no other genuinely buildable item remains open in `docs/claude-overnight-goal.md`'s ticket list — the only open items are BIO-03/BIO-04 (blocked on real BioStar API access), OPS-01's actual live-hosting execution (blocked on a real hosting/VPS decision), and real Android/iPhone device acceptance for PWA-01 (blocked on physical device access) — all correctly recorded as `blocked` in `implementation-plan.md`, not silently skipped.

### 2026-09-22 — Claude Code, overnight execution: implementation-plan.md status refresh (OPS-01)

- **Scope:** the documentation-accuracy half of OPS-01 — `docs/implementation-plan.md`'s master backlog Status column was stale (every P0/P1 row still read `not-started` despite roughly a full session's worth of real, tested, committed work). Refreshed every REQ-FND/EMP/DEV/ATT/TSH/PAY/AST/PRJ/TSK/JRN/NTF row against real evidence found on disk right now (a real migration/model/controller/route/Vue page/passing test) — never from memory or from another doc's own claims alone. REQ-EMP/DEV/ATT/TSH/PAY/most of PAY were already correctly marked from an earlier pass and left alone. P2+ backlog-only groups (REQ-MAT/REQ-CRM/REQ-EXT) were sanity-checked as still correctly untouched, not deeply re-audited.
- **Rows changed, by group:** REQ-FND 25/25 → `done`. REQ-AST: 10 → `done`, 2 → `in-progress` (REQ-AST-02 QR-scan resolution route was never built despite the `qr_token` column existing; REQ-AST-07's damage/loss/write-off half is real but standalone `Maintenance` vendor/due-date scheduling UI isn't), 1 left `not-started` (REQ-AST-10 — no dedicated report views exist, only the underlying queryable data). REQ-PRJ 4/4 → `done`. REQ-TSK: 10 → `done`, 1 → `in-progress` (REQ-TSK-03's "pre-enabled self-close for low-risk tasks" toggle was never built — submit-for-acceptance is the only path). REQ-JRN 5/5 → `done`. REQ-NTF: 8 → `done`, 3 → `in-progress` (REQ-NTF-04's bounded task-list pre-cache for offline *reading*, REQ-NTF-05's explicit quota-exceeded handling, REQ-NTF-09's explicit queue size bound — none were built; PWA-01's real scope was submission/replay, not these), 1 → `blocked` (REQ-NTF-11: real Android/iPhone device acceptance — no physical device this session, and the ticket's own wording explicitly excludes emulation as sufficient). REQ-PAY-12/13 (CSV export) correctly left `not-started` — genuinely never built.
- **Examples of rows deliberately NOT marked `done` despite closely related work existing** (to show this was evidence-checked, not rubber-stamped): REQ-AST-02 (QR token column exists, no resolution route — a real, checkable gap); REQ-NTF-04 (the offline submission queue is real and tested, but that is not the same claim as "own tasks are pre-cached for offline viewing," which nothing builds); REQ-TSK-03 (task submission/acceptance is fully real, but the specific "self-close for low-risk tasks" sub-feature named in the requirement text was never implemented — a different, narrower claim than "the task workflow works").
- **Files changed:** `docs/implementation-plan.md` (Status column only, no requirement text/IDs touched).
- **Not committed/pushed** — left for the parent session to review.
- **Next:** remaining backlog per `docs/claude-overnight-goal.md`: BIO-03/BIO-04 remainders (blocked, real BioStar API access), OPS-01's own live-hosting execution (blocked, needs a real hosting decision), REQ-NTF-11's real-device acceptance (blocked, no physical device). The concrete, still-buildable gaps this pass surfaced (QR-scan resolution, Maintenance scheduling UI, Asset reports, task self-close toggle, offline task pre-cache/quota/bound) are all real, small, well-scoped follow-ups if anyone wants to pick them up next.

### 2026-09-22 — Claude Code, overnight execution: NOTIFY-01 fully closed out (scheduled Telegram digest)

- **Scope:** the deferred remainder of NOTIFY-01 — a scheduled Telegram digest, on top of the already-built manual "send me a report now" flow. Closes NOTIFY-01 out entirely.
- **Design:** new `php artisan telegram:send-digest` (scheduled `dailyAt('08:30')` in `routes/console.php`, offset from `notifications:notify-due-items`'s own 08:00 slot). For every organization, for every ACTUALLY linked `TelegramLink` (`linked_at` not null — a pending code is never sent to), attempts every `TelegramReportType` once per calendar day and lets `SendTelegramReportAction`'s own existing authorization re-check (already 2FA-aware for the financial summary) decide which go out — no second, duplicated authorization check. Reuses that Action's existing `TelegramReportDelivery` history/retry mechanism entirely; this command creates nothing beyond what a manual request would already create. Dedup is a direct `whereDate('created_at', today)` check per `(telegram_link_id, report_type)` — that table has no separate `dedup_key` column, unlike the in-app `notifications` table. Gets a system actor (QUEUE-01's `GetOrCreateSystemActorAction`, one lazy `get-or-create` call per organization that actually has a linked user) to attribute each scheduled send's `requested_by_user_id` to, rather than `null`.
- **Real bug found and fixed while writing this command's own multi-organization test (not from code review):** the command set `CurrentOrganization`/the Postgres `app.current_org_id` GUC per organization (matching `attendance:process-incremental`/`devices:health-check`'s existing pattern) but never set spatie/laravel-permission's own "team id" (`PermissionRegistrar::setPermissionsTeamId()`) — normally set per-request by `SetCurrentOrganization` middleware, which a console command never runs through. Without it, `SendTelegramReportAction`'s `$recipient->can(...)`/`Gate::forUser(...)` checks resolved against whichever organization's team id happened to be set LAST across the whole process, not the organization actually being iterated — every organization except the last one in a multi-tenant run got a false permission result. Fixed by setting the team id alongside the existing two mechanisms. `App\Domain\Notifications\Support\UsersWithPermission` (used by `devices:health-check`) already handled this correctly internally, so that command was never affected — this was specific to the new command calling a permission check directly.
- **Files changed:** new `app/Console/Commands/TelegramSendDigest.php`; `routes/console.php` (+1 scheduled entry); new `tests/Feature/Notifications/TelegramDigestTest.php` (5 tests).
- **Contract/schema changes:** none — reuses the existing `telegram_report_deliveries` table.
- **Tests/gates:** `php artisan test --compact` → **285 tests, 282 passed / 3 skipped** (1397 assertions). `vendor/bin/phpstan analyse` (whole app) → **0 errors**. `vendor/bin/pint --dirty --test` → clean. `npm run types:check` → clean (no frontend files touched by this ticket).
- **Real multi-org isolation proof:** a dedicated test creates two organizations, each with its own linked user/permission, runs the command once, and asserts (via a deliberate `withoutTenantScope()` cross-tenant test read, mirroring `TenantIsolationRlsTest`'s own pattern) that each organization's `TelegramReportDelivery` row carries its own correct `organization_id` and neither leaked into the other — passed only after the team-id fix above; failed cleanly beforehand with exactly the symptom that fix explains.
- **Note on this session's shared working tree:** a concurrent process was mid-edit on `NavigationService.php`/`AppSidebar.vue`/mobile-nav files throughout, and a separate parallel session was finishing FILES-01's Contractor-evidence remainder (see the entry above) — both left untouched here, per this file's own no-interference protocol.
- **Next:** remaining backlog per `docs/claude-overnight-goal.md`: BIO-03/BIO-04 remainders (blocked on real BioStar API access — no live server/docs available to this session), OPS-01 execution (blocked on a real hosting/VPS decision this session cannot make on the user's behalf). No other genuinely buildable ticket remains open as of this entry.

### 2026-09-22 — Claude Code, overnight execution: FILES-01 done (closes out — Contractor act evidence)

- **Scope:** the deferred remainder of FILES-01 — Contractor act evidence preview/download. The Task-side half was already done (2026-09-21 entry below); this closes the ticket out entirely.
- **Real gap found:** `ContractorActResource` already exposed an `evidence` array (id/filename/mime_type/status) but with no URL at all, and no route existed to stream a contractor act's evidence file — a reviewer had no way to open the proof before accepting/returning an act.
- **Fix:** new protected route `GET contractors/{contractor}/acts/{act}/attachments/{attachment}` → `ContractorActController::showAttachment()`, mirroring `TaskController::showAttachment()`'s exact shape. Evidence attachments are owned by the `Contractor` (not the act itself — see `UploadContractorAttachment`), so the real ownership check is `abort_unless` the attachment id appears in the act's own `evidence_attachment_ids` array (plus still belonging to this contractor) rather than a direct owner_type/owner_id match against the act, which would never be true by this domain's own design. Served inline (not force-download), matching the Task-side convention. `ContractorActResource` now carries a real `url` per evidence item, omitted for anything not `available`. `Contracts/Show.vue` renders real clickable/thumbnail previews instead of a bare count.
- **Files changed:** `app/Http/Controllers/Contractors/ContractorActController.php` (+`showAttachment()`), `app/Http/Resources/Contractors/ContractorActResource.php`, `app/Domain/Contractors/Models/ContractorAct.php` (+`@property list<string> $evidence_attachment_ids` — a real PHPStan error without it, not just a style nit), `routes/modules/web-contractors.php` (+1 route), `resources/js/pages/Contractors/Contracts/Show.vue`; new `tests/Feature/Contractors/ContractorActAttachmentPreviewTest.php` (3 tests, mirroring `TaskAttachmentPreviewTest.php`'s exact shape: own-evidence open, cross-act rejection, cross-organization 404).
- **Contract/schema changes:** none.
- **Tests/gates:** `php artisan test --compact --group=contractors` → 13 passed (69 assertions). Whole-app `php artisan test --compact` also run — one unrelated failure (`TelegramDigestTest`) belongs to a different concurrent in-progress session's own uncommitted work, not touched here. `vendor/bin/phpstan analyse` (whole app) → **0 errors**. `vendor/bin/pint --dirty --test` → clean. `npm run types:check` / `npm run build` → passed.
- **Next:** remaining backlog per `docs/claude-overnight-goal.md`: NOTIFY-01's scheduled Telegram digest (appears to be in progress concurrently, per the `TelegramDigestTest`/`TelegramSendDigest` files already in the tree), BIO-03/BIO-04 remainders (blocked on real BioStar API access), OPS-01 execution (blocked on a real hosting decision).

### 2026-09-22 — Claude Code, overnight execution: QUEUE-01 fully closed out (system actor, incremental attendance, device health-check)

- **Scope:** the deferred remainder of QUEUE-01 this session's own earlier pass recorded as a concrete next step — a "system actor" design decision, then `attendance:process-incremental` and a device health-check command, both scheduled. Closes out QUEUE-01 entirely (the relay/RLS half was already done and closed earlier this session).
- **The system-actor design decision:** a dedicated, non-loginable `is_system_account` User per organization (mirrors ADMIN-01's `is_platform_admin` precedent exactly — a durable typed column, excluded from `User::$fillable`, settable only via one Action). `App\Domain\Auth\Actions\GetOrCreateSystemActorAction::execute($organizationId)` is idempotent (looks up an existing row first), creates it with `is_active = false` (which `ActiveUserProvider` already turns into a genuine, tested inability to log in — not just a UI convention) and an `@internal.invalid`-TLD email (the real IANA-reserved "never deliverable" TLD, used deliberately). Excluded from `UserAccessController`'s admin user list and `ProjectController`'s membership-assignment picker — the two real places a stray "user" could otherwise leak into a human-facing list.
- **`attendance:process-incremental`** (scheduled every 5 minutes): iterates every real organization, and within each one only re-runs `ReconstructAttendanceSessionsAction` for an employee who actually has a `RawAccessEvent` newer than their own new `AttendanceIncrementalCheckpoint.last_processed_at` (first-ever run per employee looks back a bounded default of 3 days, not their entire history) — attributing the run to that organization's system actor, never a null gap. Correctly sets/restores `app.current_org_id` per organization inside the loop (the same discipline this session's `SendTimesheetEmailJob`/`CompleteTelegramLinkAction` already established), proven directly by a two-organization test asserting each org gets its OWN system actor and OWN checkpoint rows, never mixed, and that no ambient tenant context survives the command.
- **`devices:health-check`** (scheduled every 15 minutes): catches a device/connector that has gone entirely SILENT (no heartbeat past a configurable threshold) — a case `RecordDeviceHeartbeatAction`'s existing reactive `device_fault` notification (NOTIFY-01) cannot catch by construction, since that only runs when a heartbeat DOES arrive. Reuses the same `device_fault` notification type with a distinct `dedup_key` prefix so the two triggers never collide, deduped per calendar day (proven: two consecutive runs against the same still-stale device produce exactly one notification, not two).
- **Files changed:** new migration `2026_09_22_100000_add_system_account_and_incremental_checkpoints.php` (additive `users.is_system_account`, additive `attendance_incremental_checkpoints` table + RLS); new `app/Domain/Auth/Actions/GetOrCreateSystemActorAction.php`; new `app/Domain/Attendance/Models/AttendanceIncrementalCheckpoint.php` + factory; new `app/Console/Commands/{AttendanceProcessIncremental,DevicesHealthCheck}.php`; `routes/console.php` (+2 scheduled entries); `app/Models/User.php` (+cast/docblock, excluded from fillable); `app/Http/Controllers/Admin/UserAccessController.php`, `app/Http/Controllers/Projects/ProjectController.php` (exclude system accounts from user-facing lists); new `tests/Feature/Shared/SystemActorAndSchedulingTest.php` (13 tests).
- **Contract/schema changes:** additive migration, applied to the real dev DB (`--pretend` previewed first).
- **Tests/gates:** `php artisan test --compact` → **274 passed / 3 skipped** (1364 assertions). `vendor/bin/phpstan` (scoped to every file this ticket touched) → 0 errors — whole-app phpstan currently reports pre-existing errors belonging to the concurrent Stocktake session's own in-progress files, not this ticket's. `pint --dirty` on touched files → clean. `npm run types:check`/`build` → passed (no frontend files touched).
- **Not committed** — left staged/unstaged in the working tree for the parent session to review, per its explicit instruction.
- **Next:** with QUEUE-01, ASSETS-01 (including Stocktake) closed, remaining backlog per `docs/claude-overnight-goal.md`: BIO-03/BIO-04's real-API-access-blocked halves, NOTIFY-01's scheduled Telegram digest, OPS-01 execution (real hosting decision required), real Android/iPhone PWA device acceptance.

### 2026-09-22 — Claude Code, overnight execution: ASSETS-01/Stocktake done (closes out ASSETS-01)

- **Scope:** the Stocktake (physical inventory count reconciliation) workflow this session's own earlier ASSETS-01 pass explicitly deferred as a distinct, separate ticket's worth of work. Closes out ASSETS-01 entirely.
- **The hard rule, enforced end to end:** "სკანირება პირდაპირ არ ცვლის საბუღალტრო ნაშთს" — recording a physical count (`ScanStocktakeLineAction`) is a pure observation, proven by a dedicated test that asserts `quantity_on_hand` is UNCHANGED immediately after a count that differs from expected. Only `ApproveStocktakeVarianceAction` — a separate, explicitly-authorized step — ever mutates the ledger, via the already-schema'd `StocktakeAdjustment` record (this table already existed from an earlier scaffold pass with no action writing to it yet).
- **Workflow:** `StartStocktakeAction` snapshots real expected quantities for every asset currently located at the chosen site (`AssetLocation.is_current`) into both `Stocktake.expected_snapshot` and one `StocktakeLine` per asset. `ScanStocktakeLineAction` records a count once; a recount creates a NEW line referencing the original via `recount_of_line_id` rather than overwriting it (immutable count history). `ApproveStocktakeVarianceAction` resolves a variance one of three ways (`quantity_correction` for quantity/consumable assets — corrects `quantity_on_hand` to the counted value; `marked_lost` — sets an individually-tracked asset's `condition` to `written_off`, reusing the same terminal state `DecideAssetIncidentAction` already uses rather than inventing a separate "lost" condition value; `confirmed_found` — reverses a previously-lost asset back to `good`), always inside a locked (`lockForUpdate`) transaction matching `FinalizeIssueAction`'s established discipline. `CompleteStocktakeAction` refuses to close a session while any line is either uncounted or has an unresolved variance.
- **Two independent permissions, separation of duties:** `assets.stocktakes.perform` (start a session, record counts) vs `assets.stocktakes.approve` (resolve a variance, complete the session) — `owner`/`warehouse_keeper` hold both, `project_manager` holds only `perform` (may count during a site walkthrough, cannot unilaterally resolve a variance). Both enforced independently, tested directly (a `project_manager`-only actor's direct `approve-variance` request is 403'd).
- **Files changed:** new `app/Domain/Assets/Actions/{StartStocktakeAction,ScanStocktakeLineAction,ApproveStocktakeVarianceAction,CompleteStocktakeAction}.php`; new `app/Policies/StocktakePolicy.php` (registered in `AssetsModuleServiceProvider`); `database/seeders/modules/AssetsPermissionsSeeder.php` (+2 permissions); new `app/Http/Controllers/Assets/StocktakeController.php`; new `app/Http/Requests/Assets/{StartStocktakeRequest,ScanStocktakeLineRequest,ApproveStocktakeVarianceRequest}.php`; new `app/Http/Resources/Assets/StocktakeResource.php`; `routes/modules/web-assets.php` (+5 routes), `config/modules/assets-nav.php` (+1 entry, reuses the existing `clipboard-list` icon rather than touching the shared `navIcons.ts` map); new `resources/js/pages/Assets/Stocktakes/{Index,Show}.vue`; `resources/js/pages/Assets/Index.vue` (+1 link); `app/Domain/Assets/Models/Stocktake.php` (`@property` docblock, Larastan inference only); new `tests/Feature/Assets/StocktakeWorkflowTest.php` (10 tests).
- **Contract/schema changes:** none — `stocktakes`/`stocktake_lines`/`stocktake_adjustments` already existed from the original scaffold pass, unused until now.
- **Tests/gates:** `php artisan test --compact` → **274 passed / 3 skipped** (1364 assertions; `assets` group alone: 19 passed, 70 assertions). `phpstan analyse` → 0 errors (fixed 2 real ones along the way: `bccomp()` needs a genuine `numeric-string`, not just any `string` — a plain `(string)` cast on a decimal-cast Eloquent attribute doesn't satisfy that narrower type, fixed with `number_format(...)`; `StocktakeResource` needed an `@mixin Stocktake` annotation, matching every other Resource in this codebase). `pint --dirty` → clean. `npm run types:check`/`build` → passed.
- **Real test proving the hard rule (not just asserted in prose):** `test('a raw count NEVER changes quantity_on_hand before approval — the hard rule')` — counts a 5-unit asset as 3, then asserts `$asset->fresh()->quantity_on_hand` is still exactly `5.0` and `variance_approved_adjustment_id` is still `null`, before any approval action runs.
- **Note on this session's shared working tree:** a concurrent process was mid-edit on `NavigationService.php`/`AppSidebar.vue`/mobile-nav files throughout this task and was left untouched, per this file's own no-interference protocol. Separately, `vendor/bin/pint --dirty` (run as part of this ticket's own gate) auto-fixed two files that belong to a different concurrent QUEUE-01 pass (`app/Domain/Auth/Actions/GetOrCreateSystemActorAction.php`, `tests/Feature/Shared/SystemActorAndSchedulingTest.php`) — harmless style-only fixes (import order, parentheses), not reviewed or committed as part of this entry.
- **Next:** remaining backlog per `docs/claude-overnight-goal.md`: NOTIFY-01's scheduled Telegram digest, BIO-03/BIO-04 remainders (blocked on real BioStar API access), OPS-01 execution (blocked on a real hosting decision). QUEUE-01's system-actor slice appears to be in progress concurrently (see the note above).

### 2026-09-22 — Claude Code, overnight execution: PROJECT-01 done (Kanban transitions + calendar)

- **Scope:** Wave 5, ticket PROJECT-01's two remaining named gaps: the Dashboard Kanban board's drag-to-transition was a deliberate no-op (`@move="() => {}"`), and no calendar view existed anywhere. Everything else in PROJECT-01 (members/WBS/documents/activity tabs) was already done in an earlier session.
- **Kanban:** `Dashboard.vue`'s `handleMove()` maps a drag between the board's 3 columns to the ONE real existing Task Action it actually corresponds to — never a new generic "set status" endpoint. Column-pair mapping: `todo(status=assigned) → in_progress` calls `projects.tasks.start` directly; `in_progress(status=in_progress, not blocked) → done` opens a small optional-comment dialog then calls `projects.tasks.submit`; every other pair (including anything touching a `blocked`/`draft` card, or a backwards move) is rejected client-side with an explicit message. `KanbanBoard.vue` is purely presentational (renders straight from the `tasksByColumn` prop, never mutates a card's column itself), so an invalid move needs no "revert" — nothing moves until the server confirms and `reloadBoard()` (`router.reload({only:['tasks','kpis']})`) re-derives from fresh props. Also fixed a real latent bug: `tasksByColumn` was a plain object built once at script-setup time, not a `computed()` — it would never have reflected a reload's fresh `tasks` prop.
- **Calendar:** new `DashboardController::calendar()` reuses the EXACT same visibility scoping as `index()` (project membership + `TaskPolicy::scopeVisibleToPerformer()`) — no separate, looser query. New `resources/js/pages/Projects/Calendar.vue`: a plain CSS-grid month view (no calendar library dependency, matching this codebase's established preference), tasks by `due_at`, click-through to the real Task page. New route `tasks.calendar`, new nav entry in `config/modules/projects-nav.php`.
- **Files changed:** `app/Http/Controllers/DashboardController.php` (+`calendar()`), `routes/modules/web-projects.php` (+1 route), `config/modules/projects-nav.php` (+1 nav entry), `resources/js/pages/Dashboard.vue` (Kanban wiring + reactive `tasksByColumn` fix), new `resources/js/pages/Projects/Calendar.vue`, new `tests/Feature/Projects/KanbanAndCalendarTest.php` (5 tests).
- **Contract/schema changes:** none.
- **Tests/gates:** `php artisan test --compact` → **258 passed / 2 skipped** (1313 assertions). `phpstan` → 0 errors. `pint --dirty`/`--test` on touched files → clean. `npm run types:check`/`build` → passed.
- **Note on test coverage:** since Kanban itself adds no new backend endpoint (it calls the existing, already-tested `start`/`submit` routes), the new test file verifies the real server-side authorization boundary those routes provide (a plain employee cannot drag-transition a task that isn't theirs — a 403 hitting the real route directly, not just "the UI hides the handle") and a non-`assigned` task's `start` attempt being rejected server-side, plus the calendar endpoint's scoping (employee sees only their own tasks, owner sees all, cross-organization isolation).
- **Next:** remaining backlog per `docs/claude-overnight-goal.md`: Stocktake (Assets, deferred), NOTIFY-01's scheduled Telegram digest, TENANT-01/BIO-03/BIO-04 remainders (external-dependency-blocked), OPS-01 execution.

### 2026-09-22 — Claude Code, overnight execution: TENANT-01 next slice done (company scoping for Sites/Employees, Devices inherit via Site)

- **Scope:** Wave 1, ticket TENANT-01 — implements exactly the next slice this file already recorded as deferred (see the 2026-09-21 "Deferred (partially investigated): TENANT-01" entry below for the original finding — not repeated here).
- **What was built:** additive nullable `sites.company_id` and `employees.company_id` (composite FK to `(organization_id, id)` on `companies`, copied exactly from `projects.company_id`'s own migration pattern — a company from a different organization can never be assigned even by request tampering). Every existing row stays NULL — no backfill guessed. `Device` deliberately gets NO own `company_id` column: it already has a real `site_id`, so `Device::resolvedCompanyId()` resolves transitively via its site instead of duplicating the fact. New shared `App\Domain\Devices\Support\CompanyScope::allows()` — the single rule every company-scoped Policy method now calls: a NULL `company_id` (unmapped) stays visible to anyone who could already see it via `organization_id` alone (company-scoping only ever tightens visibility once a row is actually assigned, never hides pre-existing data the moment this migration landed); `owner`/`system_admin` see across every company in their organization by design. Wired into `DevicePolicy::view/update`, `EmployeePolicy::view/update` (self-view and foreman-of-employee stay unaffected — those are narrow relationship grants, not the broad roster-visibility permission this scoping tightens), and a new `SitePolicy`. New minimal `SiteController`/`Devices/Sites/Index.vue` — an admin-visible "unmapped sites" list (sorted unmapped-first) with a company-assignment action, reusing the Devices module's existing `devices.view`/`devices.manage` permissions (no new permission pair).
- **Files changed:** `database/migrations/2026_09_22_090000_add_company_id_to_sites_and_employees_tables.php`; `app/Domain/Devices/Models/{Site,Device}.php`, `app/Domain/Employees/Models/Employee.php`; new `app/Domain/Devices/Support/CompanyScope.php`; `app/Policies/{DevicePolicy,EmployeePolicy}.php`, new `app/Policies/SitePolicy.php`; new `app/Http/Controllers/Devices/SiteController.php`, `app/Http/Requests/Devices/AssignSiteCompanyRequest.php`; `routes/modules/web-devices.php`, `config/modules/devices-nav.php`, `resources/js/lib/navIcons.ts` (+`map-pin`, shared file); new `resources/js/pages/Devices/Sites/Index.vue`; new `tests/Feature/Devices/TenantCompanyScopeTest.php` (6 tests).
- **Contract/schema changes:** additive migration, applied to the real dev DB (`php artisan migrate --force`, previewed with `--pretend` first).
- **Tests/gates:** `php artisan test --compact` → **253 passed / 2 skipped** (1276 assertions). `vendor/bin/phpstan analyse` (whole app, the project's own configured paths) → 0 errors. `vendor/bin/pint --dirty` → clean (on this ticket's own files). `npm run types:check`/`npm run build` → passed. New test explicitly covers two companies in ONE organization (not just two organizations, which the existing RLS suite already covered): an employee/device assigned to Company A is invisible to a Company-B-scoped user holding only the plain `.view` permission; `owner` still sees across both; a device correctly inherits its company via its site; the unmapped-sites list and assignment action both work; a cross-organization company id is rejected by the request's own validation rule.
- **Explicitly not done, deferred:** Projects/Devices policies beyond `view`/`update` (e.g. list/index-level filtering by company) were not swept — this slice proves the mechanism end-to-end (schema + the shared `CompanyScope` rule + two real Policies + the admin assignment UI), matching the "split an oversized ticket into recorded slices" instruction this session has followed throughout; extending `CompanyScope::allows()` to any additional Policy is now a small, mechanical follow-up, not a design question.
- **Next:** none of Wave 1 remains open after this. Remaining backlog: BIO-03/BIO-04 (blocked on real BioStar API access), QUEUE-01's incremental-processing/health-check scheduling (needs the "system actor" design decision), NOTIFY-01's scheduled Telegram digest, Stocktake (Assets), OPS-01 execution.

### 2026-09-21 — Claude Code, overnight execution: PWA-01 done (offline draft/replay for My Day)

- **Scope:** Wave 5, ticket PWA-01 ("reliable mobile operation" — task draft/replay wired to the existing manifest/service-worker/offlineQueue primitives; IndexedDB durability; interrupted-send recovery; failed/conflict resolution; logout data clearing).
- **Real bug fixed in the existing primitive:** `resources/js/lib/offlineQueue.ts`'s `runInStore()` resolved on `request.onsuccess` instead of `tx.oncomplete` — the UI could report "saved" a moment before IndexedDB actually committed, risking silent loss on the ticket's own "app restart" scenario. Fixed to resolve only on real commit.
- **What was built:** `resources/js/lib/taskOfflineSync.ts` (enqueue/replay wiring, single-pass per-kind dispatch — deliberately does NOT reuse `offlineQueue.replayQueued()`'s generic per-kind callback pattern twice, which would mis-mark an unsent item of the other kind as sent); `MyDay.vue` now enqueues photo/task-submission actions when offline (`start`/`block`/`unblock` stay online-only — the `offline_sync_submissions.kind` enum is closed to `comment`/`photo`/`task_submission`, a deliberate scope match to the already-designed schema, not an oversight); new `/api/v1/projects/{project}/tasks/{task}/offline-{attachments,submissions}` endpoints (`App\Http\Controllers\Api\V1\Tasks\OfflineSyncController`) that call the exact same `UploadTaskAttachment`/`SubmitTaskForAcceptance` Actions the desktop Inertia routes use and record a durable `OfflineSyncSubmission` row (`applied`/`conflict`/`for_review`/`rejected`) for every attempt; a "needs review" section on My Day for `for_review` rows with an acknowledge action; `clearProtectedData()` wired into logout (`UserMenuContent.vue`, click intercepted so the wipe genuinely finishes before the navigation starts).
- **Two significant, previously-latent infrastructure bugs found and fixed while building this ticket's own real-browser Playwright test (not from code review — from an actual 401 then 404 hitting the new endpoint):**
  1. `bootstrap/app.php` never called `$middleware->statefulApi()` — every `/api/v1/...` route already written with `auth:sanctum` (this ticket's new routes, and `api-dailyjournal.php` before it) assumed Sanctum's SPA stateful-session mode was active, but it never was; a real browser session always 401'd. Fixed by adding `statefulApi()`.
  2. Once that exposed a second layer: `App\Providers\Auth\AuthModuleServiceProvider::insertMiddlewareAfter()` mutates `$kernel->middlewareGroups` via reflection but never calls the Kernel's own `syncMiddlewareToRouter()` — `Illuminate\Routing\Router` keeps a SEPARATE copy of the middleware groups that request-time routing actually reads. This had been silently masked for the `web` group this whole session by an accidental side effect: the `api` group's own (separate, public-method-based) prepend calls happened to trigger a full re-sync of every group as a side effect. The moment this ticket needed `insertMiddlewareAfter()` for the `api` group too (to fix Sanctum's own session-vs-tenant-context ordering), that accidental safety net disappeared for BOTH groups simultaneously — confirmed directly: `SetCurrentOrganization`'s own code stopped running at all, RLS silently returned nothing, a real linked Employee record read back as "not found" for the account that owns it. Fixed by calling the Kernel's protected `syncMiddlewareToRouter()` explicitly (via reflection) at the end of `insertMiddlewareAfter()`, rather than continuing to depend on an unrelated call's side effect. Full `php artisan test` (247 passed / 2 skipped) re-run clean after this fix — it was never caught by the existing suite because `actingAs()` bypasses real guard/middleware resolution entirely.
- **Real end-to-end proof, not just automated tests:** a Playwright script (not checked into the repo — ad hoc, see the parent session's own report for the exact steps) logged in as a real seeded user, used real Chromium network-level offline emulation, submitted a task through the actual UI while offline, confirmed the item was durably queued in IndexedDB (inspected directly), reloaded the page while still offline (the SW correctly served the static offline fallback, proving no Vue app is required to survive that reload), restored connectivity, reopened the app, and confirmed the queued item replayed automatically, landed as a real `submitted` Task with a real `TaskSubmission` row, and the `OfflineSyncSubmission` audit row read back `status=applied`. Left two harmless real fixture rows (`PWA E2E Test Project` / `PWA offline test task`) in the real dev DB from this proof — the task now has a real submission, so it can't be hard-deleted without violating a foreign key (by design); delete via the normal Projects UI if you want them gone.
- **Files changed:** `resources/js/lib/offlineQueue.ts` (bug fix), new `resources/js/lib/taskOfflineSync.ts`, `resources/js/pages/MyDay.vue`, `resources/js/components/UserMenuContent.vue`; new `app/Http/Controllers/Api/V1/Tasks/OfflineSyncController.php`, new `routes/modules/api-tasks.php`; `app/Http/Controllers/MyDayController.php` (+`organizationId`/`userId`/`offlineReviewItems` props, +`acknowledgeOfflineReview()`), `routes/modules/web-shared.php` (+1 route); `bootstrap/app.php` (+`statefulApi()`), `app/Providers/Auth/AuthModuleServiceProvider.php` (the router-sync fix, api-group ordering fix); new `tests/Feature/Shared/OfflineSyncSubmissionTest.php` (6 tests).
- **Contract/schema changes:** none — `offline_sync_submissions`/its RLS already existed, unused, from an earlier scaffold pass.
- **Tests/gates:** `php artisan test --compact` → 247 passed / 2 skipped (1254 assertions). `phpstan` → 0 errors. `pint --dirty` → clean. `npm run types:check`/`build` → passed.
- **Explicitly NOT done, honestly:** real Android Chrome / iPhone Safari device acceptance (the ticket's own explicit acceptance list) — no physical device available in this session, same limitation `docs/runbook.md`'s own PWA section already flags for install-testing. Quota-failure and multi-tab-conflict scenarios were not separately exercised. `start`/`block`/`unblock` staying online-only is a recorded scope boundary, not a gap.
- **Next:** real-device QA once hardware is available; if `comment`-kind offline support is ever wanted from My Day specifically (no UI path for it there today, only bundled into submit), extend `taskOfflineSync.ts` the same way `photo`/`task_submission` were added.

### 2026-09-21 — Navigation usability pass

- Fixed the mobile bottom destinations: `ჩემი დღე` now opens `/my-day`; Projects, Timesheets and Inventory are shown only when the corresponding destination exists in the user's server-filtered `navGroups`; Profile opens `/settings/profile`. Removed the old incorrect shortcuts (`projects` labeled as tasks, devices labeled as tools, employees labeled as notifications).
- Added the notification bell to the mobile header and put `ჩემი დღე`/`ჩემი პროფილი` together in the desktop `ჩემი სამუშაო` group.
- Removed AppSidebar's static fallback links, which could show modules regardless of the user's permissions. NavigationService now orders groups for day-to-day use while preserving server-side permission filtering.
- Verification: `npm run types:check` passed; `git diff --check` passed. Browser surface inventory returned no connected tabs, so visual/device-width browser acceptance remains pending.
- Files touched: NavigationService, shared-nav config, architecture navigation note, AppSidebar, BottomNav, MobileTopBar and `mobileNav.ts`. Claude's in-progress MyDay/offline-sync files were left untouched.

### 2026-09-21 — Claude Code, overnight execution: NOTIFY-01 done (notifications, global search, Telegram reporting)

- **Scope:** Wave 5, ticket NOTIFY-01 ("შეტყობინებები და მფლობელის Telegram"). Ran concurrently with the ASSETS-01/TIMESHEET-EMAIL-02 sessions (no file overlap — confirmed via `git status` before every write). Full detail in `docs/claude-overnight-progress.md`.
- **Part A — real in-app notifications:** the `notifications`/`notification_preferences` schema, `NotificationType`, and `SensitivePreviewGuard` already existed from an earlier scaffold pass but had zero call sites creating a `Notification` row anywhere. Wired all 6 named P1 types to real events: `task_assigned` (`AssignTask`), `task_returned` (`ReturnTaskSubmission`), `mention` (`AddComment`, never self-notifying the author), `device_fault` (`RecordDeviceHeartbeatAction`, only on a fresh transition INTO offline/degraded, not every heartbeat while it stays faulty), `timesheet_exception` (`ReconstructAttendanceSessionsAction::flagAnomaly()`, only on a genuinely new — not deduplicated — anomaly), and `overdue`/`tool_return_due` via a new daily-scheduled `notifications:notify-due-items` command (both are "true about the current time," not discrete write events, so need a periodic scan rather than a hook). Every creation path goes through one shared `NotificationCreator::create()` that always checks the recipient's mute preference and always runs `SensitivePreviewGuard::assertSafe()` first — no call site can skip either. New `NotificationController` (own-notifications-only, IDOR-free by construction, same pattern as `MyProfileController`) + `NotificationBell.vue` polling every 30s, mounted in the shared `AppSidebarHeader.vue`.
- **Part B — global search, real backend for an existing UI stub:** `resources/js/components/GlobalSearch.vue` was already built (trigger, `Cmd/Ctrl+K`, debounced query emission) but never wired to anything (its own docblock said so, pointing at DEC-048). New `GlobalSearchController` covers Projects/Tasks/Employees/Devices/Companies — each branch is a deliberate, documented COPY of that entity's own index controller's real scoping/Policy check (e.g. Tasks reuses `TaskPolicy::scopeVisibleToPerformer()` exactly as `DashboardController` already does), never a reimplementation — a search result the user could not otherwise open must never appear, since existence alone is a real disclosure. Covered by `GlobalSearchTest.php`'s negative assertions (a project_manager never finds a project they're not a member of; a user never finds another organization's project).
- **Part C — Telegram, mocked transport only, real linking logic:** per the ticket's explicit constraint, read-only reporting, no license/door/payment commands. New `TelegramTransportInterface` + `FakeTelegramTransport` (the only implementation built — mirrors the `FakeAccessControlProvider`/`SimulatorDeviceAdapter` precedent for an external boundary with no live credentials), bound in a new `NotificationsModuleServiceProvider`. `link_code` is deliberately globally unique (not per-org) because a real bot webhook completing the handshake has no tenant context yet — `CompleteTelegramLinkAction` uses `withoutTenantScope()` then explicitly sets `app.current_org_id` from the row it found, the identical pattern `AuditLogger` already established for the same pre-tenant-context problem. `SendTelegramReportAction` re-checks the recipient's CURRENT permission (or, for `financial_summary`, the same 2FA-aware `access-financial-data` Gate used everywhere else) immediately before formatting each send — a denied check never even creates a `queued` delivery row. A real webhook endpoint does not exist (would need the same cross-tenant-lookup problem QUEUE-01 solved, genuinely deferred rather than built unreviewed for a transport with no live bot) — `completeDemoLink()` is a `local`/`testing`-only route (mirrors the existing `design-system/*` QA-route precedent) so the linking flow is demonstrable/testable end to end without ever exposing an unauthenticated cross-tenant route in production.
- **Deliberate scope boundaries, recorded not silently dropped:** no scheduled Telegram digest (only user-initiated "send me a report now" — the ticket's own wording doesn't demand a schedule); Contractors are skipped for `task_assigned`/`task_returned` (a Contractor has no `user_id`/login account in this codebase, nothing to notify in-app); no HTTP-level test for `NotificationController`'s own endpoints specifically (the underlying `NotificationCreator`/`Notification` model paths are fully covered — a thin controller wrapping them is comparatively low-risk, but this is a real, not a claimed-covered, gap).
- **Files changed:** new migration `2026_09_21_150000_create_telegram_reporting_tables.php` (see the note in the TIMESHEET-EMAIL-02 entry below about its shared timestamp prefix with that ticket's own migration — both applied fine, no actual collision, do not rename either now); new `app/Domain/Notifications/{Models/TelegramLink,Models/TelegramReportDelivery,Contracts/TelegramTransportInterface,Adapters/FakeTelegramTransport,Actions/{GenerateTelegramLinkCodeAction,CompleteTelegramLinkAction,SendTelegramReportAction},Exceptions/{TelegramLinkCodeInvalidException,TelegramReportNotAuthorizedException},Support/{NotificationCreator,TaskNotificationRecipients,UsersWithPermission,TelegramReportType}}`; new `app/Providers/Notifications/NotificationsModuleServiceProvider.php`; new `app/Console/Commands/NotifyDueItems.php` (+ scheduled in `routes/console.php`); new `app/Http/Controllers/{NotificationController,GlobalSearchController,TelegramLinkController}.php`; new `routes/modules/web-notifications.php`; modified `app/Domain/Tasks/Actions/{AssignTask,ReturnTaskSubmission,AddComment}.php`, `app/Domain/Devices/Actions/RecordDeviceHeartbeatAction.php`, `app/Domain/Attendance/Actions/ReconstructAttendanceSessionsAction.php`, `app/Domain/Shared/Models/Notification.php` (added `@property` docblock, Larastan-only), `.env.example` (+`TELEGRAM_BOT_TOKEN` empty placeholder); new `resources/js/lib/api.ts` (fetch+CSRF helper — no axios/fetch wrapper existed anywhere in this codebase before); new `resources/js/components/Notifications/NotificationBell.vue`; modified `resources/js/components/AppSidebarHeader.vue` (wires `GlobalSearch.vue`'s existing `@query`/`:results` contract to the real endpoint, unchanged prop/emit shape); new `resources/js/pages/Notifications/Preferences.vue`; new factories `TelegramLinkFactory`/`TelegramReportDeliveryFactory`; new tests `tests/Feature/Notifications/{NotificationTriggerTest,GlobalSearchTest,TelegramLinkTest}.php` (14 tests).
- **Contract/schema changes:** additive only (`telegram_links`, `telegram_report_deliveries`, both RLS-enabled — verified via `pg_policies` on the real dev DB), no new permissions (every gate reuses an existing permission/Gate).
- **Tests/gates:** `php artisan test --compact` (shared worktree, all three concurrent sessions' tests together) → **241 passed / 2 skipped** (1235 assertions); `--group=notifications` alone → 13 passed, 29 assertions. `vendor/bin/phpstan analyse` (whole app) → **0 errors** (fixed 7 real ones in my own new files — two missing `Collection<int, User>` generic annotations and a missing `@property` docblock on `Notification::$payload` that left Larastan unable to infer the cast). `vendor/bin/pint --dirty` → clean (auto-fixed import ordering/phpdoc alignment only). `npm run types:check` → clean. `npm run build` → passed.
- **Next:** PWA-01 is the last unstarted Wave 5 ticket. TENANT-01/BIO-03/BIO-04's remaining halves stay blocked on live infrastructure this session doesn't have, per their own earlier entries.

### 2026-09-21 — Claude Code, overnight execution: TIMESHEET-EMAIL-02 done (batch/bundled emailing)

- **Scope:** Wave 4, ticket TIMESHEET-EMAIL-02 ("ერთად/ჯგუფურად გაგზავნა" — batch/bundled timesheet emailing). Ran concurrently with the ASSETS-01/NOTIFY-01 sessions (no file overlap). Full detail in `docs/claude-overnight-progress.md`.
- **Built on, not instead of, TIMESHEET-EMAIL-01** (done earlier this session): its `SendTimesheetEmailAction`/`SendTimesheetEmailJob`/`TimesheetSnapshotMail` are unchanged in behavior — its own test file passes unmodified.
- **Two modes:** `per_employee` (each selected timesheet → its own separate email, batched under one new `TimesheetEmailBatch` — a deliberate simplification that reuses EMAIL-01's job untouched instead of a multi-attachment path) and `bundled` (all selected timesheets → ONE message to one chosen recipient, via a new `timesheet_email_delivery_items` table + `TimesheetBundleSnapshotMail` + `SendBundledTimesheetEmailJob`).
- **Real correctness properties, each covered by its own test:** every selected id is re-resolved/re-authorized at creation time (cross-org and permission-denied share one generic skip reason, so a rejection never discloses a foreign timesheet id exists); "select all N filtered" is re-resolved server-side at commit time (capped at 200); a version edit between enqueue and send never changes what actually gets attached; cancelling a batch only touches still-queued deliveries, never an already-sent one; a single recipient's SMTP failure never blocks or duplicates the others and only that one is retriable.
- **Files changed:** see the progress-doc entry for the full list — 3 additive migrations, 2 new models, a new `TimesheetSnapshotResolver` (extracted from EMAIL-01's own Action for reuse, behavior-identical), 3 new Actions, a new Job/Mailable/view, `TimesheetController`/`TimesheetPolicy`/routes extended, `Timesheets/Index.vue` selection UI + a new `TimesheetEmailBatchHistory.vue`, 8 new tests.
- **Heads up:** this ticket's own migration and the concurrent NOTIFY-01 session's `create_telegram_reporting_tables` migration share the identical `2026_09_21_150000` timestamp prefix — both ran fine (no actual collision), but worth renaming one for a cleaner sequential history before any future squash; do NOT rename now, both are already applied to the real dev DB under these exact names.
- **Contract/schema changes:** additive only, applied to the real dev DB (`--pretend` previewed first).
- **Tests/gates:** `php artisan test --compact` (shared worktree) → 241 passed / 2 skipped (1235 assertions); `tests/Feature/Timesheets/` alone: 23 passed. `phpstan` whole-app → 7 pre-existing errors, all in the concurrent NOTIFY-01 session's own files (confirmed via `git status`), 0 in anything this ticket touched. `pint --dirty` → clean on this ticket's files. `npm run types:check`/`npm run build` → passed.
- **Next:** Wave 4 (TIMESHEET-01/EMAIL-01/EMAIL-02) is now fully closed out. NOTIFY-01/PWA-01 remain — check NOTIFY-01's own progress entry before starting fresh, a concurrent session may already be advancing it.

### 2026-09-21 — Claude Code, overnight execution: ASSETS-01 done (issue/return/transfer/repair-loss workflows)

- **Scope:** Wave 5, ticket ASSETS-01 ("complete issue/return/transfer/repair/loss workflows with immutable custody history and concurrency safeguards"). Not product-decision-heavy — the full schema and business rules were already specified in `docs/data-model.md`'s "Domain: Assets" section and the existing models'/exceptions' own docblocks (from the initial scaffold checkpoint); this pass implements against that existing spec. Only ~2 of the needed Actions existed before this pass (`RegisterAssetAction`, `SaveIssueDraftAction`); no controllers, requests, resources, policies, nav, routes, permissions, or Vue pages existed at all.
- **What was built:** `FinalizeIssueAction` (the draft → `awaiting_receipt` transition `SaveIssueDraftAction`'s own docblock named but never implemented — locks `AssetActiveCustody` per line via `CustodyLockManager` for individually-tracked assets, or the `assets` row directly for quantity/consumable, per that lock manager's own documented split), `ConfirmCustodyReceiptAction` (`awaiting_receipt`/`in_transit` → `issued`, moves the asset's current `asset_locations` row only once receipt is actually confirmed — records only `received_confirmation_user_id`/`_at`, explicitly never claimed as a qualified e-signature, matching the model's own docblock), `TransferCustodyAction` (direct employee-to-employee/warehouse transfer as one new `type='transfer'` row — a judgment call, documented in the Action's own docblock, that only `issue` needed a two-step draft/finalize split, since nothing in the existing code/spec named a `TransferCustodyAction` draft stage), `RequestAssetReturnAction` (employee self-service, purely informational per the model's own docblock — never mutates custody), `ReturnCustodyAction` (the exact name `CustodyTransaction`'s own docblock already referenced — full/partial return as its own immutable new `type='return'` row, `damaged` condition routes the asset to `under_repair` not back to `good`), `ReportAssetIncidentAction`/`DecideAssetIncidentAction` (a `write_off` decision creates a real `Approval` row using the shared `enum('approved','rejected')` vocabulary — copying JOURNAL-01's own bug-fix pattern from earlier this session rather than repeating that exact mistake — and never deletes the `Asset` row). New `assets.*` permission set (`AssetsPermissionsSeeder`, primary role `warehouse_keeper` per the spec's own role table; self-service confirm-receipt/request-return need no permission at all, ownership-checked in `CustodyTransactionPolicy` the same way `MyProfileController` gates "your own data"). Full Assets/{Index,Create,Show}.vue with inline issue/transfer/return/incident forms.
- **Concurrency, proven not just implemented:** a real feature test issues an asset, then attempts a second `FinalizeIssueAction` on the same still-reserved asset and asserts `AssetAlreadyIssuedException` — the DB-level `SELECT ... FOR UPDATE` + partial unique index (`asset_active_custody_locked_unique`, already present in the original schema migration) backs this. Genuine multi-process parallel-request testing was not built (same explicitly documented limitation as this session's own MONEY-01 ticket) — only the sequential-guard case is proven by an automated test.
- **A real, documented schema-boundary judgment call:** `asset_locations.locatable_id` is NOT NULL, but there is no `warehouses` table yet (P2 scope, per the original Assets migration's own docblock) — so a return with no named destination warehouse leaves the asset with no current `asset_locations` row at all (an honest "location unspecified" state) rather than inventing a placeholder id. Documented in `ReturnCustodyAction`'s own docblock.
- **Explicitly deferred, not attempted:** `Stocktake`/`StocktakeLine`/`StocktakeAdjustment` (physical-count reconciliation) — `docs/data-model.md` describes this as its own distinct variance-approval workflow ("სკანირება პირდაპირ არ ცვლის საბუღალტრო ნაშთს"), a second ticket's worth of work, not silently folded into this one. `Maintenance` scheduling UI beyond what an incident's `repair` decision naturally implies.
- **Files changed:** new `app/Domain/Assets/Actions/{FinalizeIssueAction,ConfirmCustodyReceiptAction,TransferCustodyAction,RequestAssetReturnAction,ReturnCustodyAction,ReportAssetIncidentAction,DecideAssetIncidentAction}.php`; new `database/seeders/modules/AssetsPermissionsSeeder.php` (registered in `AggregatingPermissionsSeeder::ORDER`); new `app/Policies/{AssetPolicy,CustodyTransactionPolicy,AssetIncidentPolicy}.php`; new `app/Providers/Assets/AssetsModuleServiceProvider.php`; new `app/Http/Controllers/Assets/{AssetController,CustodyTransactionController,AssetIncidentController}.php`; new `app/Http/Requests/Assets/*` (6 files); new `app/Http/Resources/Assets/{AssetResource,CustodyTransactionResource,AssetIncidentResource}.php`; new `routes/modules/web-assets.php`; new `config/modules/assets-nav.php`; `resources/js/lib/navIcons.ts` (+`package`, shared file, noted per the no-interference protocol); new `resources/js/pages/Assets/{Index,Create,Show}.vue`; small `@property` docblock additions to `AssetLocation`/`AssetIncident` models (Larastan datetime-cast inference, same pre-existing pattern as `CredentialAssignment.php`/`Task.php` elsewhere in this codebase — no behavior change); new `tests/Feature/Assets/AssetCustodyWorkflowTest.php` (10 tests).
- **Contract/schema changes:** none — no migration. The full schema already existed from the initial scaffold checkpoint; only the new `assets.*` permissions needed seeding (re-ran `AggregatingPermissionsSeeder` against the real dev DB, idempotent).
- **Tests/gates:** `php artisan test --compact` → **220 passed / 2 skipped** (1168 assertions — includes this ticket's 10 new tests and TIMESHEET-EMAIL-01's 7, built concurrently in the same shared worktree, see that entry below). `vendor/bin/phpstan analyse` (whole app) → **0 errors**. `vendor/bin/pint --dirty` → clean. `npm run types:check` → clean. `npm run build` → passed.
- **Real gotcha hit and worked around in the new tests, worth recording for future test authors:** the web request's own `SetCurrentOrganization` middleware clears `CurrentOrganization` on `terminate()` — every direct Eloquent query made in a test body AFTER an HTTP call must re-establish it (`CurrentOrganization::set($this->organization->id)`) or the tenant global scope fails closed and returns nothing, even for the same organization. Same `HasVersion`-default-not-refetched gotcha JOURNAL-01 already documented this session also applies to `AssetIncident->version` read immediately after `::factory()->create()`.
- **Next:** Stocktake (deferred slice above), or continue the remaining Wave 5 backlog (NOTIFY-01, PWA-01 — both still just empty placeholder directories as of the last platform audit).

### 2026-09-21 — Claude Code, overnight execution: TIMESHEET-EMAIL-01 done (single-timesheet email)

- **Scope:** Wave 4, ticket TIMESHEET-EMAIL-01 ("email one timesheet with recipient/subject preview, authorization, immutable attachment snapshot, queue, audit history and safe retry"). Builds directly on TIMESHEET-01's `GenerateTimesheetPdfAction` (on-demand, unpersisted PDF) — this ticket adds the persisted-snapshot half that Action's own docblock said was still missing, plus real Mail infrastructure (zero `Mailable`/`Mail::` usage existed anywhere in this codebase before this ticket).
- **What was built:** new `timesheet_email_deliveries` table (RLS, `status` enum strictly `queued`/`sent`/`failed` — never `delivered`, per the overnight goal's explicit "don't claim SMTP delivery" constraint); `SendTimesheetEmailAction::send()`/`retry()` resolve-or-create an immutable `Attachment` snapshot per `(timesheet_id, version)` — reused across repeat sends of the same version, a fresh one generated only after a real version bump — create/update a delivery row, and dispatch `SendTimesheetEmailJob` (ShouldQueue) **only after the DB transaction that created the delivery row has committed**, never from inside it (see below). `TimesheetSnapshotMail` attaches the stored snapshot bytes directly (never regenerates). `TimesheetController` gained `emailPreview`/`emailSend`/`emailRetry`, gated on a new `TimesheetPolicy::send()` ability (reuses the existing `timesheets.timesheets.approve` permission — owner/finance only, deliberately stricter than plain `view`). `Timesheets/Show.vue` gained a send form + delivery-history list with a retry button per failed row.
- **Two real bugs found and fixed while writing this ticket's own tests, not guessed:** (1) dispatching the queue job from *inside* the same `DB::transaction()` that creates the delivery row is unsafe under `QUEUE_CONNECTION=sync` (this codebase's own test environment, and a plausible small-deployment production config) — the job would run synchronously mid-transaction, and its own failure-path write (marking the delivery `failed`) would be rolled back along with everything else the instant it threw, destroying the very failure record it was trying to create. Fixed by moving `SendTimesheetEmailJob::dispatch()` to run only after the transaction closure returns. (2) `TimesheetSnapshotMail` originally implemented `ShouldQueue` — since `Illuminate\Mail\PendingMail::send()` silently redirects to `queue()` for any `ShouldQueue` mailable, the job's `Mail::to(...)->send(...)` call was actually just re-queuing the mail (never contacting the transport) and then immediately marking the delivery `sent` regardless. Removed `ShouldQueue` from the Mailable — `SendTimesheetEmailJob` is itself the queued unit of work, the Mailable it sends from inside that job must run synchronously.
- **A third fix, found the same way:** `SendTimesheetEmailJob::handle()`'s original `finally` block unconditionally called `CurrentOrganization::clear()` — correct for a real async worker process (which starts with no ambient context) but wrong under `QUEUE_CONNECTION=sync`, where the job runs synchronously inside whatever request/test already had its own tenant context set, and clearing would silently wipe that caller's context out for the remainder of the same request. Fixed to save/restore the previous value instead of unconditionally clearing.
- **Files changed:** new migration `2026_09_21_140000_create_timesheet_email_deliveries_table.php`; new `app/Domain/Timesheets/Models/TimesheetEmailDelivery.php`, `app/Domain/Timesheets/Actions/SendTimesheetEmailAction.php`, `app/Domain/Timesheets/Exceptions/EmailDeliveryNotRetryableException.php`; new `app/Jobs/Timesheets/SendTimesheetEmailJob.php`; new `app/Mail/TimesheetSnapshotMail.php` + `resources/views/mail/timesheet-snapshot.blade.php`; new `database/factories/TimesheetEmailDeliveryFactory.php`; `app/Domain/Attendance/Models/Timesheet.php` (+`emailDeliveries()`); `app/Http/Controllers/Timesheets/TimesheetController.php` (+3 actions); new `app/Http/Requests/Timesheets/SendTimesheetEmailRequest.php`, `app/Http/Resources/Timesheets/TimesheetEmailDeliveryResource.php`; `app/Policies/TimesheetPolicy.php` (+`send()`); `routes/modules/web-timesheets.php` (+3 routes); `resources/js/pages/Timesheets/Show.vue` (+email section); new `tests/Feature/Timesheets/TimesheetEmailTest.php` (7 tests).
- **Contract/schema changes:** additive migration, applied to the real dev DB (previewed with `--pretend` first).
- **Tests/gates:** `php artisan test --compact` → **210 passed / 2 skipped** (1122 assertions; was 203/2/1094 before this ticket — +7 new tests, others from the concurrent JOURNAL-01/ASSETS-01 sessions in the same shared worktree). `vendor/bin/phpstan analyse` (whole-app) → 6 pre-existing errors, all in `app/Http/Controllers/Assets/*`/`app/Http/Resources/Assets/*` — confirmed via `git status` to belong to a concurrent parallel session's in-progress Assets work, not this ticket's files; **0 errors** in every file this ticket touched (verified with a scoped PHPStan run against exactly this ticket's paths). `vendor/bin/pint --dirty` → clean (auto-fixed import ordering only). `npm run types:check` → clean. `npm run build` → passed. Never sent a real email at any point — `MAIL_MAILER=array` in the test environment (`phpunit.xml`) and `Mail::fake()` in every test; no live SMTP send was attempted.
- **Deliberate scope boundary:** the recipient defaults to the timesheet's own employee's linked `User.email` when one exists (`Employee.user_id` is nullable per this codebase's "Employee and login account are separate concepts" rule) — an employee with no linked account has no default and the sender must supply an email explicitly; the form already supports this (a plain text input, not restricted to a picker).
- **Next:** TIMESHEET-EMAIL-02 (batch sending) can now reuse `SendTimesheetEmailAction`/`SendTimesheetEmailJob` per-recipient rather than rebuilding either. Real SMTP credentials are still required before any of this is live-acceptance-tested (per `docs/deployment.md`'s own security checklist).

### 2026-09-21 — Claude Code, overnight execution: JOURNAL-01 done (Daily Journal UI)

- **Scope:** Wave 5, ticket JOURNAL-01 ("implement the missing Daily Journal pages against existing routes/domain, with role checks"). Frontend-only per the ticket's own framing — the entire backend (`DailyReportController`, all 5 Domain Actions, models, Resources, FormRequests, `DailyReportPolicy`) already existed, committed since the very first interrupted checkpoint (`f7e5b48`), but `resources/js/pages/DailyJournal/` was an empty directory and zero tests existed.
- **What was built:** all 5 Vue pages the controller's own `Inertia::render()` calls already dictated — `DailyJournal/{Projects,Index,Form,Show,Revisions}.vue` — matching this codebase's established plain-list/inline-form house style (`Timesheets/Index.vue`, `Tasks/Show.vue`, `Companies/CompanyForm.vue` were the copied references, not a new DataTable abstraction). `Form.vue` is shared by create/edit; `Show.vue` gates every action button on the controller's own `can.*` booleans and posts submit/accept/return with the report's current `version` as `target_version`, showing the existing `StaleDailyReportVersionException` error inline rather than crashing.
- **Two small, additive controller fixes needed to make the UI real, not backend logic changes:** (1) `create()`/`edit()` had no way to pick which tasks to link (`task_ids` was accepted by both FormRequests but never offered a picker) — added a `tasks` prop (project's own tasks, same query shape as `teams`). (2) `index()` had no `canCreate` signal for the "new entry" button — added it via the existing `create` Policy check, same pattern as `Show`'s `can` block.
- **The real, confirmed backend bug found while writing this ticket's own first tests (not guessed, not backend scope creep — a golden-path 500):** `AcceptDailyReportAction`/`ReturnDailyReportAction` wrote `Approval::create(['decision' => 'accepted'])` / `'returned'` — but the shared `approvals.decision` column (`database/migrations/2026_09_16_090160_create_approvals_table.php`) is `enum('approved','rejected')`, the exact vocabulary every OTHER module already writes (Payroll, Timesheets — grepped and confirmed). Every real accept/return call was hitting a CHECK-constraint violation and 500ing; this had zero test coverage before today so it was never caught. Fixed both call sites to `'approved'`/`'rejected'` (the module's own `daily_journal.report.{accepted,returned}` audit action strings and `DailyReport.status` still carry the module's real terminology — only the shared table's row uses the shared vocabulary).
- **Route/nav fix:** `DailyReportController::projects()`'s own docblock claimed it was routed as `daily-journal.projects`, but that route never actually existed and `config/modules/dailyjournal-nav.php` pointed its nav entry at the literal placeholder route `'dashboard'`. Added the real top-level route and fixed the nav config — the module's documented intent is now real.
- **Files changed:** `routes/modules/web-dailyjournal.php`, `config/modules/dailyjournal-nav.php`, `app/Http/Controllers/DailyJournal/DailyReportController.php` (+`tasks`/`canCreate` props only), `app/Domain/DailyJournal/Actions/{AcceptDailyReportAction,ReturnDailyReportAction}.php` (decision-value bug fix); new `resources/js/pages/DailyJournal/{Projects,Index,Form,Show,Revisions}.vue`; new `tests/Feature/DailyJournal/DailyReportWebAccessTest.php` (6 tests: golden path create→submit→accept, manager return-with-reason, stale-version rejection, cross-project 404, denied-direct-request on every route not just hidden UI, projects-landing scoping).
- **Contract/schema changes:** none — no migration.
- **Tests/gates:** `php artisan test --compact` → **203 passed / 2 skipped** (1094 assertions; +8 net from the pre-JOURNAL-01 baseline — 6 new tests here, others from concurrent parallel work in the same tree, see below). `vendor/bin/phpstan analyse` (whole-app) → 4 pre-existing errors, all in `app/Domain/Timesheets/Actions/GenerateTimesheetPdfAction.php` — **not this ticket's files**, confirmed via `git status` to belong to a concurrent parallel session's in-progress Timesheet PDF work; zero errors in anything JOURNAL-01 touched. `vendor/bin/pint --dirty` → clean. `npm run types:check` → clean (fixed several real `useForm()` error-key type mismatches where the server's error bag key, e.g. `version`, doesn't match the form's own submitted field name, e.g. `target_version` — a `serverErrors`/`versionError()` typed-cast helper, not a suppression). `npm run build` → passed.
- **Note on a parallel session:** this pass ran concurrently with another agent actively building Timesheet PDF export (`app/Domain/Timesheets/Actions/GenerateTimesheetPdfAction.php` and related files appeared mid-session, untouched by this ticket, per the no-interference protocol's file-ownership rule).
- **Next:** ADMIN-02 is also done as of this same session (see entry below) — Wave 3 and this Wave 5 quick-win are both closed. Remaining: Wave 4 (TIMESHEET-01/EMAIL-01/EMAIL-02) — TIMESHEET-01's PDF half appears to be in progress concurrently; PROJECT-01/ASSETS-01/NOTIFY-01/PWA-01 remain unstarted.

### 2026-09-21 — Claude Code, overnight execution: TIMESHEET-01 done (PDF snapshot, on-demand)

- **Scope:** Wave 4, ticket TIMESHEET-01 ("usable filtering/selection and permission-aware timesheet detail; reproducible PDF snapshot with Georgian text/fonts and period/version identification"). Filtering/selection/detail already existed from this session's earlier Timesheets-module work (`TimesheetController::index/show`, confirmed by reading before adding anything) — this pass built only the PDF half, which did not exist at all (no PDF library in `composer.json`, no Georgian-capable font anywhere in the repo).
- **What was built:** `composer require barryvdh/laravel-dompdf` (v3.1.2). Sourced a real, glyph-complete Georgian font: extracted `NotoSansGeorgian-{Regular,Bold}.ttf` from the `@fontsource/noto-sans-georgian` npm package's WOFF2 files (decompressed via Python's `fonttools`; the npm package itself was installed with `--no-save`, used only as a one-time source, then `npm uninstall`ed — confirmed `package.json`/`package-lock.json` unchanged). New `App\Domain\Timesheets\Support\TimesheetPdfFonts::register()` registers both weights with dompdf's `FontMetrics` (idempotent, safe to call every render). New `App\Domain\Timesheets\Actions\GenerateTimesheetPdfAction` renders `resources/views/pdf/timesheet.blade.php` (org/employee/period/status/version header, per-line table, footer with generation timestamp + version — the "period/version identification" the ticket names) and streams it. New route `GET timesheets/{timesheet}/pdf` → `TimesheetController::pdf()`, reusing the existing `view` Policy ability (no new permission). `Content-Disposition: inline` (barryvdh's `stream()` default) so it opens in-browser, matching this session's FILES-01 inline-preview precedent. `Timesheets/Show.vue` gained a plain PDF link (no new `can` prop needed — anyone who can already view the HTML page can view its PDF).
- **Verified Georgian rendering for real, not just "it generated a file" (the ticket's own explicit ask, and the exact failure mode a font mis-registration produces silently — no exception, just empty boxes):** generated a real PDF with a Georgian employee name (`გიორგი მაისურაძე`) and Georgian labels, then extracted its actual PDF text layer with `pdftotext` (xpdf/poppler) — the extracted text came back as correct, real Georgian (`ტაბელი`, `ორგანიზაცია თანამშრომელი პერიოდი...`, the exact employee name), proving dompdf embedded a working ToUnicode CMap for the font, not tofu boxes. Recorded the full technique in `docs/decisions.md` DEC-086. The new `tests/Feature/Timesheets/TimesheetPdfTest.php` automates this same `pdftotext` extraction-and-assert as its 4th test, with an honest `test()->skip(...)` (not a silent pass) if `pdftotext` isn't present in a given environment.
- **Deliberate scope boundary, recorded for TIMESHEET-EMAIL-01's benefit:** this PDF is generated **on-demand and streamed**, never persisted. TIMESHEET-EMAIL-01's own wording ("immutable attachment snapshot") needs a stored, versioned copy for a mail attachment — that storage layer does not exist yet and must be added there.
- **Files changed:** `composer.json`/`composer.lock` (+barryvdh/laravel-dompdf and its transitive deps); new `config/dompdf.php` (published, defaults kept — `font_dir`/`font_cache` already default to `storage_path('fonts')`); new `storage/fonts/{NotoSansGeorgian-Regular.ttf,NotoSansGeorgian-Bold.ttf,.gitignore}` (the `.gitignore` keeps only the two source TTFs tracked, not dompdf's generated hashed-cache copies — those regenerate on first use via `TimesheetPdfFonts::register()`'s idempotent call); new `app/Domain/Timesheets/Support/TimesheetPdfFonts.php`, `app/Domain/Timesheets/Actions/GenerateTimesheetPdfAction.php`; `app/Http/Controllers/Timesheets/TimesheetController.php` (+`pdf()`); `routes/modules/web-timesheets.php` (+1 route); new `resources/views/pdf/timesheet.blade.php`; `resources/js/pages/Timesheets/Show.vue` (+PDF link); new `tests/Feature/Timesheets/TimesheetPdfTest.php` (4 tests); `docs/decisions.md` (+DEC-086).
- **Contract/schema changes:** none — no migration.
- **Tests/gates:** `php artisan test --compact` → **203 passed / 2 skipped** (1094 assertions — shared-worktree total with the concurrent JOURNAL-01/ADMIN-02 sessions; `tests/Feature/Timesheets/` alone: 8 passed, 0 failed, run in isolation to confirm no cross-session interference). `vendor/bin/phpstan analyse` (whole-app) → **0 errors** (4 real errors found and fixed in this ticket's own new `GenerateTimesheetPdfAction.php` — Larastan correctly flagged unnecessary nullsafe operators on relations backed by NOT NULL FKs and a redundant `??` on an array covering the full status-enum union; fixed by using `findOrFail`/plain property access instead of suppressing). `vendor/bin/pint --dirty` → clean. `npm run types:check` → clean. `npm run build` → passed.
- **Note on a parallel session:** ran concurrently with the JOURNAL-01 session in the same working tree, per the user's explicit multi-agent instruction — no file overlap by design (confirmed via `git status` before every write); JOURNAL-01's own handoff entry above independently confirms it saw and correctly attributed this ticket's now-fixed PHPStan errors rather than touching them.
- **Next:** TIMESHEET-EMAIL-01 (single-timesheet emailing — needs a persisted/immutable PDF snapshot first, see scope boundary above) and TIMESHEET-EMAIL-02 (batch emailing); real SMTP credentials remain an external prerequisite for live-send acceptance, not for building/testing the feature itself (use Mail fake, per the overnight goal's own instruction).

### 2026-09-21 — Claude Code, overnight execution: ADMIN-02 done (user/role/permission-override administration)

- **Scope:** Wave 3, ticket ADMIN-02 ("configurable functionality groups, roles and user overrides with documented precedence and data scopes. Expose effective permissions and audit changes. Test both hidden UI and denied direct requests"). Delivered as one bounded, deliberately-scoped vertical slice rather than the full ticket wording — see "Deliberately out of scope" below for what's still open.
- **Scope decisions made, not guessed:** (1) "Functionality groups" = permissions grouped by their existing `module.` name prefix (new `App\Domain\Auth\Support\PermissionGroups`), not a second parallel grouping concept. (2) Role→permission grants stay **code/seeder-owned this pass** — the new `/admin/roles` page is explicitly read-only, mirroring how this session's own TENANT-01 ticket was deliberately scoped down rather than rushed; making the 11-role catalog's grants runtime-editable is recorded as future work, not attempted here. (3) "User overrides" = two independent per-user-per-organization mechanisms: a **grant override** (spatie's own native `givePermissionTo()`/`model_has_permissions` — no new schema) and a **deny override** (new `user_permission_denials` table + Actions, since spatie has no native "deny" concept).
- **Documented precedence, most authoritative first:** (1) an explicit deny-override — always wins, even over a platform admin; (2) a direct grant-override; (3) a role-derived permission (the baseline); (4) ADMIN-01's platform-admin bypass.
- **A genuinely non-obvious implementation finding, worth remembering for any future Gate/spatie work in this codebase:** a plain `Gate::before` registered in `App\Providers\AppServiceProvider::boot()`, even registered *before* ADMIN-01's platform-admin one, does **not** reliably win against spatie/laravel-permission's own permission resolution. `spatie/laravel-permission`'s `PermissionRegistrar::registerPermissions()` registers its own `Gate::before` the moment `Gate::class` is first resolved from the container — and that resolution is triggered by the very first `Gate::before(...)` facade call in application code, so spatie's callback always ends up *first* in the callback list regardless of source-code order, and it returns `true` immediately for any role/direct-granted permission, short-circuiting before an app-level `Gate::before` ever runs. Confirmed empirically (a `Gate::before` deny-check that provably never fired despite being "registered first" in the code). **The actual fix:** override `App\Models\User::hasPermissionTo()` (spatie's own resolution entry point, which its `Gate::before` calls internally via `checkPermissionTo()`) to consult the deny-list first — this works regardless of Gate callback ordering, since it intercepts at the source spatie itself queries. The app-level `Gate::before` (in `AppServiceProvider`) is kept as a second layer for abilities that aren't registered permission names at all (so spatie's callback has nothing to grant and defers), which is what makes deny still beat the platform-admin bypass for that case.
- **What was done:** additive migration for `user_permission_denials` (RLS, unique per org+user+permission); `App\Domain\Auth\Models\UserPermissionDenial`; `App\Domain\Auth\Support\{PermissionGroups,PermissionDenialCache}` (the latter a per-process, per-user+org memoization cache, explicitly busted by the two mutating Actions after every write — required for correctness, not just performance, since a stale cache would let a just-added deny appear not to apply within the same request/process); six `App\Domain\Auth\Actions\*` (`AssignUserRoleAction`, `RemoveUserRoleAction` — reason-required, refuses self-removal, refuses removing `owner`/`system_admin` from the last user in the org who holds either — mirrors `RevokePlatformAdminAction`'s last-admin guard; `Grant`/`RevokeUserPermissionOverrideAction`; `DenyUserPermissionAction` — reason-required, refuses self-deny; `RemoveUserPermissionDenialAction`), all audited via the existing `AuditLogger`; `App\Policies\UserAccessPolicy`; `App\Http\Controllers\Admin\{RoleController,UserAccessController}`; new `auth.permissions.override` permission (owner/system_admin only, distinct from the pre-existing but previously-unwired `auth.roles.assign`/`auth.users.view`); `Admin/Roles/Index.vue`, `Admin/Users/{Index,Show}.vue`.
- **Deliberately out of scope, not silently assumed done:** role-permission grants remain code-owned (see above); **no per-project/per-company data-scope overrides** — everything here is organization-level only, matching every other permission check in this codebase today; no bulk role-assignment UI (one user at a time).
- **Files changed:** see the corresponding `docs/claude-overnight-progress.md` entry for the complete file list (26 new files, 3 modified: `app/Providers/AppServiceProvider.php`, `app/Models/User.php`, `database/seeders/modules/AuthPermissionsSeeder.php`, plus `resources/js/lib/navIcons.ts` — shared file, flagged here per the no-interference protocol, added the `shield-check` icon key only).
- **Contract/schema changes:** additive migration `2026_09_21_130000_create_user_permission_denials_table.php`, applied to the real dev DB (`--pretend` previewed first, RLS policy confirmed via `pg_policies`); `auth.permissions.override` permission seeded to the real DB via the idempotent `AggregatingPermissionsSeeder`.
- **Tests/gates:** `php artisan test --compact` → **195 passed / 2 skipped** (1021 assertions; was 184/2/953 before this ticket — +11 new tests in `tests/Feature/Auth/UserAccessAdminTest.php`). `vendor/bin/phpstan analyse` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check` → clean. `npm run build` → passed.
- **Next:** JOURNAL-01 (Wave 5 — `DailyJournal` already has a working backend, only UI + tests missing, the fastest remaining win) or Wave 4 (TIMESHEET-01/EMAIL-01/EMAIL-02, entirely unstarted).

### 2026-09-21 — Claude Code, overnight execution: OPS-01 done (deployment docs)

- **Scope:** Wave 5, ticket OPS-01 (deployment/security/performance documentation, scheduler/workers/monitoring, backup and isolated restore procedure). Documentation only — no host was touched, nothing was deployed, per the ticket's own explicit constraint. Prompted directly by the user asking how to connect their existing on-prem BioStar2 server to a VPS-hosted deployment "so information updates continuously live."
- **What it documents (not new architecture — this formalizes what `services/device-connector` already does, per DEC-080/081/082's real, hardware-verified BioStar2 integration):** the device-connector runs on-prem at each site with LAN access to that site's BioStar2 server and connects OUTBOUND ONLY to the VPS's public HTTPS API using a per-site Sanctum machine token (`tokens:issue-machine`, already existed) — no VPN or inbound port-forward needed on any site's LAN, only outbound 443 to the VPS domain. Also covers: nginx+php-fpm vs. `artisan serve` for the web process (with an honest fallback unit for a minimal deployment), real systemd units for the queue worker and the scheduler (the thing that actually runs QUEUE-01's `outbox:relay` schedule in production — nothing runs it without this), TLS/`APP_URL`/`SESSION_SECURE_COOKIE` for a real domain, a security checklist that explicitly re-states the non-negotiable "never grant Postgres superuser/BYPASSRLS to the app's runtime role" constraint and "`BIOSTAR_WRITE_DISPATCH_ENABLED` stays false until a real hardware pilot confirms the write path" (BIO-01), what to monitor (connector `/health`, the BIO-03 import-health panel, queue `failed_jobs`, the scheduler timer itself), and a backup/restore procedure — explicitly flagged as a recommendation only, since no backup automation exists in this codebase yet.
- **Files changed:** new `docs/deployment.md`; new `infra/systemd/oda-crm-queue.service`, `infra/systemd/oda-crm-schedule.service`, `infra/systemd/oda-crm-schedule.timer`, `infra/systemd/oda-crm-web-fallback.service`.
- **Contract/schema changes:** none — no application code touched.
- **Tests/gates:** not run — this ticket touches no application code, nothing to regression-test. No host was deployed to or modified.
- **Explicitly left as open decisions, not guessed (extends `docs/decisions.md` § Open business-policy questions #6):** hosting provider, self-hosted vs. managed Postgres/Redis, backup storage destination. SMTP provider is explicitly deferred further — irrelevant until TIMESHEET-EMAIL-01/02 (Wave 4) are actually implemented, which they are not yet.
- **Next:** ADMIN-02 (Wave 3, still the next item in wave order) or JOURNAL-01 (Wave 5 — `DailyJournal` already has a working backend controller/actions/routes committed since the interrupted `f7e5b48` checkpoint, but `resources/js/pages/DailyJournal/` is an empty directory and `tests/Feature/DailyJournal/`/`tests/Unit/DailyJournal/` don't exist — the fastest real win of the remaining backlog since no backend design work is needed, only UI + tests).

### 2026-09-21 — Claude Code, overnight execution: FILES-01 done (Task scope)

- **Scope:** Wave 3, ticket FILES-01, completed for Task photos/attachments — the ticket's concretely-described core gap. Contractor act evidence has the same underlying issue but no UI to view it exists yet — deferred with a recorded next step, not silently half-fixed. Full detail in `docs/claude-overnight-progress.md`.
- **Real gap found:** `TaskDetailResource` exposed attachment metadata with no URL at all, and no route existed to stream a task's file — `Tasks/Show.vue` rendered attachments as unclickable filename text. Worse: `SubmitTaskForAcceptance` re-owns evidence attachments from the Task to the `TaskSubmission` on submit, so a submitted task's photos disappeared from the page entirely (not just unopenable) — a reviewer had no way to see evidence before accepting/returning.
- **Fix:** new `TaskSubmission::photoAttachments()` relation (live resolution, not the historical `photo_attachment_ids` array); new protected route `GET projects/{project}/tasks/{task}/attachments/{attachment}` with an ownership check requiring the attachment belong to that task directly or one of its own submissions (never merely exist somewhere); served inline (not force-download) so images embed and PDFs open in-browser; `TaskDetailResource` now carries a real `url` on both `attachments[]` and each submission's `photos[]`; `Tasks/Show.vue` renders real clickable/thumbnail previews. Separately verified (already correct, no fix needed): a failed submit already left the task `in_progress` with attachments intact and retryable.
- **Files changed:** `app/Domain/Tasks/Models/TaskSubmission.php`, `app/Http/Controllers/Tasks/TaskController.php`, `app/Http/Resources/Tasks/TaskDetailResource.php`, `routes/modules/web-projects.php`, `resources/js/pages/Tasks/Show.vue`, new `tests/Feature/Projects/TaskAttachmentPreviewTest.php` (4 tests).
- **Contract/schema changes:** none.
- **Tests/gates:** `php artisan test --compact` → 184 passed / 2 skipped (953 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check`/`npm run build` → passed.
- **Next:** ADMIN-02 (large, product-decision-heavy) or remaining Wave 3/4/5 tickets per the runbook's ordered queue.

### 2026-09-21 — Claude Code, overnight execution: WORKER-01 done (Wave 3 started)

- **Scope:** Wave 3, ticket WORKER-01 (real "ჩემი დღე" / My Day). Full detail in `docs/claude-overnight-progress.md`.
- **Real gap:** `resources/js/pages/MyDay.vue` still rendered a static `demoTasks` placeholder array — every employee saw identical fake tasks regardless of who was logged in; the `my-day` route was a bare `Route::inertia()` with no backend.
- **Fix:** new `App\Http\Controllers\MyDayController` reuses the existing `App\Policies\TaskPolicy::scopeVisibleToPerformer()` scope (same one FIX-02/A3 already proved correct) to build 4 real buckets — today, overdue, in-review, returned (identified via the task's own latest `TaskSubmission.status`, since a task itself has no "returned" status — it reverts to `in_progress`). `MyDay.vue` fully rewritten: every action (start/comment/photo/submit/unblock/report-blocker) posts straight to the pre-existing `TaskController` routes, no new mobile business logic. Fixed a real stale-reference bug caught during testing: the bottom sheet must re-derive its task from current (possibly just-reloaded) props by id, not cache a snapshot object, or a just-uploaded photo's attachment id never reaches the subsequent submit call.
- **Files changed:** new `app/Http/Controllers/MyDayController.php`; `routes/modules/web-shared.php`; `resources/js/pages/MyDay.vue` (full rewrite); new `tests/Feature/Shared/MyDayTest.php` (4 tests).
- **Contract/schema changes:** none.
- **Tests/gates:** `php artisan test --compact` → 180 passed / 2 skipped (923 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check`/`npm run build` → passed.
- **Next:** FILES-01 (protected attachment preview/download, multi-photo upload polish) or ADMIN-02 (large — administration area/roles/feature-toggle system, genuinely product-decision-heavy).

### 2026-09-21 — Claude Code, overnight execution: QUEUE-01 done (partial), Wave 2 closed out

- **Scope:** Wave 2, ticket QUEUE-01 (outbox relay/scheduler tenant-context correctness). Full detail, including a real Postgres-RLS surprise worth reading, in `docs/claude-overnight-progress.md`. This closes out Wave 2 (BIO-01 ✅, BIO-02 ✅ card slice, BIO-03 ⏸ partial, BIO-04 ✅, QUEUE-01 ⏸ partial — every deferred half has a recorded next step, nothing silently dropped).
- **Real bug found, verified against a REAL restricted Postgres role (not just SQLite):** `RelayOutboxEvents`/`ProcessOutboxEventJob` both used `OutboxEvent::withoutTenantScope()` (Eloquent-layer only) but `outbox_events` also carries a real RLS policy requiring `app.current_org_id` — which neither ever set before their first read. Under `oda_app` (non-superuser, FORCE RLS), this means the relay would see **zero rows from any tenant, always** — confirmed empirically with a standalone reproduction before fixing, not assumed.
- **A second, non-obvious finding while fixing it:** a `FOR SELECT`-only RLS policy does NOT satisfy `SELECT ... FOR UPDATE`'s row-locking visibility (empirically confirmed, contradicted my first assumption) — needed a second, `FOR UPDATE`-classified policy with explicit `WITH CHECK (false)` so the escape hatch can make a row visible/lockable across tenants but can never itself authorize the actual write.
- **Fix:** two new narrowly-scoped, additive RLS policies on `outbox_events` gated on a distinct `app.outbox_relay_active` GUC (never set by any web/user-input path, only the relay/job's own server-side code, always transaction-scoped) — NOT a BYPASSRLS/superuser grant, which the standing constraint forbids. `RelayOutboxEvents`/`ProcessOutboxEventJob` (including its `failed()` callback, which had the identical bug) now use a two-phase read-then-narrow-to-real-tenant pattern. `outbox:relay` is now actually scheduled in `routes/console.php` (it existed but nothing ever ran it).
- **Explicitly deferred:** scheduling "incremental attendance processing" and "health checks" — needs a "system actor" design decision (how does an automated job attribute a locked-period `AttendanceAdjustment` or an audit entry, which currently require a real `User`?) that doesn't exist anywhere in this codebase yet. Recorded with a concrete next step rather than invented under time pressure. Separately confirmed already-true: the dashboard never triggers a synchronous reconstruction.
- **Files changed:** new migration `2026_09_21_120000_add_system_relay_read_policy_to_outbox_events_table.php`; `app/Console/Commands/RelayOutboxEvents.php`; `app/Jobs/Shared/ProcessOutboxEventJob.php`; `routes/console.php`; new `tests/Feature/Shared/OutboxScheduleTest.php`; `tests/Feature/Auth/TenantIsolationRlsTest.php` (+1 real-Postgres-RLS test).
- **Contract/schema changes:** additive migration (2 new RLS policies only), applied to the real dev DB, verified via `pg_policies`.
- **Tests/gates:** `php artisan test --compact` → 176 passed / 2 skipped (871 assertions) — including a real-Postgres-RLS test that runs the actual relay/job code (not mocks) against the restricted `oda_app` role and proves the fix. `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean.
- **Next:** Wave 3 (ADMIN-02, WORKER-01, FILES-01) per the runbook's ordered queue.

### 2026-09-21 — Claude Code, overnight execution: BIO-03 (partial) + BIO-04 done

- **Scope:** Wave 2, tickets BIO-03 (partial — admin import-health visibility) and BIO-04 (connection reliability). Full detail in `docs/claude-overnight-progress.md`. Deliberately NOT attempted: BIO-03's real event search/pagination confirmation and BIO-04's event-code taxonomy mapping — both require confirming real Suprema API behavior this session has no access to; guessing either risks silently mis-processing a real security event log. Recorded as concrete deferred next steps, not silently skipped.
- **BIO-03 done:** new "მოვლენების იმპორტის მდგომარეობა" section on `Devices/Show.vue` — real checkpoint position, open (unresolved-only) `data_gap`/`out_of_order_events`/`clock_drift` anomaly counts, and `DeviceSyncCommand` status backlog, all built from already-durable data with zero new tables.
- **BIO-04 done:** removed the process-wide `NODE_TLS_REJECT_UNAUTHORIZED=0` mutation in `SupremaDeviceGatewayAdapter` — installed `undici` (a real new dependency, resolved in `docs/decisions.md`'s Pending dependencies table) and now scope TLS trust per-adapter via a dedicated `Agent` passed as `dispatcher`; added `BIOSTAR_CA_CERT_PATH` (trust the BioStar server's own cert specifically) and `BIOSTAR_REQUEST_TIMEOUT_MS` (default 10s, via `AbortSignal.timeout`); added exponential backoff (capped at 5 min) to the connector's tick loop for repeated failures instead of hammering an unreachable server every poll interval; hardened `heartbeat()`'s online-status check against a string/number JSON type variation.
- **Files changed:** `services/device-connector/{package.json,package-lock.json,src/adapters/suprema-device-gateway.js,src/index.js,README.md,tests/connector.test.js}`; `app/Http/Controllers/Devices/DeviceController.php`; `resources/js/pages/Devices/Show.vue`; `tests/Feature/Devices/DeviceWebAccessTest.php`; `docs/decisions.md`.
- **Contract/schema changes:** none. New real npm dependency `undici` ^7.29.1.
- **Tests/gates:** `php artisan test --compact` → 174 passed / 2 skipped (855 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check`/`npm run build` → passed. `cd services/device-connector && npm test` → 20/20.
- **Next:** QUEUE-01 (outbox relay/scheduler, tenant-safe job processing), closing out Wave 2.

### 2026-09-21 — Claude Code, overnight execution: BIO-02 done (card-mapping slice)

- **Scope:** Wave 2, ticket BIO-02 (external identifier mapping/triage), completed for the `card` external-identifier type — the case that actually affects attendance correctness. `device`/`user` mapping types are schema-ready (the `external_type` enum already lists them, the collision-safe unique key already covers them) but have no ingestion/confirm action wired — deliberately deferred since no second real BioStar source exists in this deployment to design that flow against honestly. Full detail in `docs/claude-overnight-progress.md`.
- **Real gap found:** `IngestRawAccessEventAction` already never auto-created an Employee for an unrecognized card and already preserved the raw event immutably with `credential_id = null` — but there was no admin-visible triage page at all (only discoverable via a manual DB query), and `ReconstructAttendanceSessionsAction::orderedEventsFor()` had no way to retroactively attribute those historical null-credential_id rows once a human later identified the card's real owner.
- **Fix:** new `external_identifier_mappings` table (source-scoped, collision-safe — `source_instance_key` defaults to `'default'`, never nullable, so a future second BioStar source can't silently collide via Postgres's NULL-is-distinct uniqueness semantics); `IngestRawAccessEventAction` now registers a deduped `pending` triage row per still-unknown card; new admin page `Devices/ExternalMappings/Index.vue` to confirm (→ reuses `IssueCredentialAction` itself, so the same active-assignment/uniqueness guards apply) or ignore a row; `ConfirmExternalIdentifierMappingAction` immediately re-runs `ReconstructAttendanceSessionsAction` so confirming means attendance is correct right away; `orderedEventsFor()` now also resolves ownership for historical unmatched rows via a confirmed mapping, without ever rewriting the immutable `RawAccessEvent` row itself (verified: raw rows are byte-for-byte identical before/after confirmation in the new test).
- **Files changed:** see `docs/claude-overnight-progress.md`'s BIO-02 entry for the full list — new migration, model, factory, 2 exceptions, 2 actions, 1 policy, 1 controller, 2 requests, 1 Vue page, provider/seeder/routes/nav wiring, and a `resources/js/lib/navIcons.ts` addition (`shield-question` — shared file, noted here per the no-interference protocol).
- **Contract/schema changes:** additive migration `2026_09_21_110000_create_external_identifier_mappings_table.php`, applied to the real dev DB (RLS policy verified via `pg_policies`); new `devices.external_mappings.{view,manage}` permissions seeded to the real DB.
- **Tests/gates:** `php artisan test --compact` → 173 passed / 2 skipped (839 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check` and `npm run build` → passed (a fresh Vue page needs a rebuild before an HTTP-level Inertia test can render it — hit and fixed this once).
- **Next:** BIO-03/BIO-04 (event pagination/checkpoint + taxonomy normalization + TLS/timeout hardening), then QUEUE-01, closing out Wave 2.

### 2026-09-21 — Claude Code, overnight execution: BIO-01 done (Wave 2 started)

- **Scope:** Wave 2, ticket BIO-01 (enforce read-only BioStar integration through server and adapter boundaries). Full detail in `docs/claude-overnight-progress.md`.
- **Real gap found:** the Node connector's `SupremaDeviceGatewayAdapter::applyCommand()` (`services/device-connector/src/adapters/suprema-device-gateway.js`) already implements a real, unconditional `add_user` write against a live BioStar2 server with no config gate — any queued `add_user` command would be sent to real hardware the moment `DEVICE_CONNECTOR_MODE=suprema` is set. The in-process PHP `SupremaGSdkAdapter` was already safe (always throws), but it isn't what actually talks to real BioStar — the Node connector is, and it had no equivalent guard.
- **Fix — two independent boundaries (hiding UI buttons alone was explicitly called out as insufficient):** (1) server-side: new `devices.biostar_write_dispatch_enabled` config (env `BIOSTAR_WRITE_DISPATCH_ENABLED`, default `false`) — `ConnectorCommandController::index()` now returns an empty `commands` array to the connector for a `suprema`-mode device while the flag is off, leaving the actual `DeviceSyncCommand` rows untouched (`pending`/`retry`, fully visible on the device's own sync-command-queue page); (2) adapter-side defense in depth: the JS adapter independently checks the same env var and refuses every command with zero HTTP calls unless it's the literal string `true`. New `BioStarReadOnlyBanner.vue` shown on the Devices/Credentials pages explaining that BioStar owns cards/access at this stage.
- **Files changed:** `config/modules/devices.php`, `app/Http/Controllers/Api/V1/Devices/ConnectorCommandController.php`, `services/device-connector/src/adapters/suprema-device-gateway.js`, `services/device-connector/README.md`, `app/Http/Controllers/Devices/{DeviceController,CredentialController}.php`, new `resources/js/components/Devices/BioStarReadOnlyBanner.vue`, `resources/js/pages/Devices/{Index,Show,Credentials/Index}.vue`, `tests/Feature/Devices/DeviceConnectorApiTest.php` (+3), `services/device-connector/tests/connector.test.js` (+2 new, 5 existing updated to explicitly opt into write dispatch rather than being weakened).
- **Contract/schema changes:** none (no migration — command withholding happens at the API response layer, not the DB).
- **Tests/gates:** `php artisan test --compact` → 166 passed / 2 skipped (799 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check` and `npm run build` → passed. `cd services/device-connector && npm test` → 15/15.
- **Explicitly out of scope, not touched:** BIO-02/03/04 (separate tickets, own file lists); the simulator "tick" button's `suprema`-mode UX (already safe — would throw, not silently fake success — just not polished).
- **Next:** BIO-02 (external identifier mapping/triage for unmatched BioStar cards/users, possibly multiple sources per organization).

### 2026-09-21 — Claude Code, overnight execution: ATT-01 done, Wave 1 complete

- **Scope:** Wave 1, ticket ATT-01 (attendance session reconstruction correctness — denied-event exclusion, historical card-reassignment attribution, overnight-shift/timezone review, anomaly-rerun dedup). This closes out Wave 1. Full detail in `docs/claude-overnight-progress.md`.
- **Real bugs fixed in `ReconstructAttendanceSessionsAction`:** (1) denied/rejected badge reads (`access_denied`) were opening/closing sessions like real granted reads — fixed by excluding a new `DENIED_EVENT_CODES` const from the event query; (2) events were attributed to an employee based on "this credential was assigned to them at some point in the requested range," not "at this specific event's own timestamp" — a mid-day card reassignment could leak events between the old and new holder — fixed with `credentialBelongedToEmployeeAt()`, re-checking `CredentialAssignment::activeAt()` per event; (3) `flagAnomaly()` created a fresh duplicate row every time reconstruction reran over the same unresolved problem — fixed by checking for an existing unresolved anomaly of the same type first (matched by session id, or by `details->raw_access_event_id` for session-less orphan events).
- **Not a bug (verified, now covered by a test):** `WorkDateResolver::startDateFor()` already correctly converts to site-local time before taking the calendar date, so overnight shifts crossing local midnight were already attributed to the correct start date — this had no regression coverage before, now does.
- **Files changed:** `app/Domain/Attendance/Actions/ReconstructAttendanceSessionsAction.php`, `tests/Feature/Attendance/AttendanceSessionReconstructionTest.php` (+4 tests).
- **Contract/schema changes:** none.
- **Tests/gates:** `php artisan test --compact` → 161 passed / 2 skipped (791 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check` and `npm run build` → passed. `npm --prefix services/device-connector test` → 13/13.
- **Wave 1 exit gate:** FIX-01, FIX-02, ADMIN-01, MONEY-01, ATT-01 all done and verified (tests + phpstan + pint + typecheck + build + connector tests, all green together at the numbers above). TENANT-01 remains deferred with a recorded next slice (add nullable `sites.company_id`, no backfill guessing, admin-visible "unmapped sites" list) — see its own entry below.
- **Next:** starting Wave 2 — BIO-01 (enforce read-only BioStar integration through server/adapter boundaries), then BIO-02 (external identifier mapping/triage for unmatched cards), BIO-03/BIO-04 (event pagination/checkpoint + taxonomy normalization + TLS/timeout hardening), QUEUE-01 (tenant-safe outbox relay/scheduler).

### 2026-09-21 — Claude Code, overnight execution: MONEY-01 done, TENANT-01 investigated and deferred

- **Scope:** Wave 1, ticket MONEY-01 (audit findings D1/D2 — payment concurrency/idempotency/ownership/currency). TENANT-01 was investigated but deferred as an oversized ticket for a dedicated pass (see `docs/claude-overnight-progress.md` for the full writeup of both).
- **MONEY-01 real bugs fixed:** neither `RecordPaymentAction` (Payroll) nor `RecordContractorPaymentAction` (Contractors) locked anything before reading the outstanding balance (a real concurrent-overpayment race); neither had idempotent-retry protection (a double-click/network-retry created a duplicate real payment); `RecordPaymentAction` selected an `Advance` by id with no check it belongs to the employee being paid; both Actions accepted a payment currency with no validation against what the balance is actually denominated in. Fixed with: employee/contract row locking inside the transaction, a client-generated `request_id` (new migration, unique per organization, wired through both forms/controllers — one has no Vue form yet, so only its backend was updated), a new `AdvanceOwnershipMismatchException`, and a strict GEL-only policy for Payroll / contract-currency-match for Contractors (per the ticket's own stated "or strictly limit currency" escape hatch).
- **TENANT-01 finding (not yet fixed, deferred):** `sites`, `devices`, and `employees` have no `company_id` at all — only `organization_id`. Within one organization with two companies, every Site/Device/Employee is currently visible/assignable to both equally. Fixing this needs a 3-table additive schema decision (own column vs. inherit via Site), a backfill strategy for existing rows (none can be auto-inferred today), and policy/query updates across two modules not touched this pass. Recorded as a concrete next slice rather than rushed.
- **Files changed:** see `docs/claude-overnight-progress.md`'s MONEY-01 entry for the full list (Payroll + Contractors payment Actions, 2 new exceptions, 1 new migration, 1 Vue form, tests).
- **Contract/schema changes:** additive migration `2026_09_21_100000_add_request_id_to_payment_tables.php`, applied to the real dev DB.
- **Tests/gates:** `php artisan test --compact` → 157 passed / 2 skipped (778 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean. `npm run types:check` and `npm run build` → passed. `npm --prefix services/device-connector test` → 13/13.
- **Explicit limitation:** genuine multi-process concurrent-request testing was not built (no test harness here spins up literal parallel DB connections) — only sequential correctness is covered by automated tests; the lock statements themselves are code-verified present.
- **Next:** ATT-01 (attendance correctness — exclude denied/system events, historical card-reassignment attribution, overnight-shift/timezone review), closing out Wave 1.

### 2026-09-21 — Claude Code, overnight execution: ADMIN-01 done

- **Scope:** Wave 1, ticket ADMIN-01 (audit finding A1 — email-string full-admin Gate bypass). Full detail in `docs/claude-overnight-progress.md`.
- **Root cause:** `AppServiceProvider`'s `Gate::before` returned `true` for every ability whenever `$user->email === 'admin@protect.ge'` — not durable (an email change would drop or transfer access to whoever holds that string next), and unconditionally bypassed `Gate::define('access-financial-data', ...)`'s MFA/2FA requirement too.
- **Fix:** new durable `users.is_platform_admin` column (migration `2026_09_21_090000`, one-time backfill for the existing `admin@protect.ge` row), excluded from `User::$fillable`. `Gate::before` now checks that column by user id, and explicitly excludes `access-financial-data` from its bypass so 2FA is still required even for a platform admin. New `App\Domain\Auth\Actions\{Grant,Revoke}PlatformAdminAction` (audited, revoke refuses to remove the last admin) and a bootstrap console command `admin:platform-admin`.
- **Files changed:** `app/Providers/AppServiceProvider.php`, `app/Models/User.php`, new `app/Domain/Auth/Actions/{Grant,Revoke}PlatformAdminAction.php`, new `app/Console/Commands/GrantPlatformAdmin.php`, new migration, new test `tests/Feature/Auth/PlatformAdminTest.php`.
- **Contract/schema changes:** additive migration (`users.is_platform_admin`, default false). Applied to the real dev DB via `php artisan migrate --force` (not `migrate:fresh`) and verified.
- **Tests/gates:** `php artisan test --compact` → 151 passed / 2 skipped (765 assertions). `vendor/bin/phpstan` → 0 errors. `vendor/bin/pint --dirty` → clean.
- **Real verification (not just unit tests):** ran the migration against the actual `oda_crm` dev database, confirmed `admin@protect.ge` retained `is_platform_admin=true`, then logged in as that account against the real running dev server and confirmed `/dashboard` and a permission-gated page (`/payroll/pay-periods`) both still return 200 post-fix.
- **Next:** TENANT-01 (Company/Site/Project/Employee/Device/financial-record ownership chain verification), then MONEY-01, ATT-01 to close Wave 1.

### 2026-09-21 — Claude Code, overnight execution: FIX-01 and FIX-02 done

- **Scope:** Wave 1, tickets FIX-01 (PHPStan) and FIX-02 (project/task visibility), per `claude-overnight-goal.md`. Full detail in `docs/claude-overnight-progress.md`.
- **FIX-01:** `Zone::devices()` had a real return-type bug (annotated `HasMany`, body called `hasManyThrough`); added missing `Door`/`Zone` factories and relation/factory generics; fixed `ProjectPolicy::delete()`'s always-true redundant condition (`hasRole('owner') && (can('projects.delete') || hasRole('owner'))` → `hasRole('owner') || can('projects.delete')`, behavior-preserving today since only `owner` holds that permission). New test: `tests/Feature/Devices/ZoneDoorDomainTest.php`.
- **FIX-02:** Two real, confirmed-by-reading-the-code access leaks. (1) `ProjectPolicy::viewAny()` treated the per-project `projects.view` permission as equivalent to org-wide `projects.viewAny`, so every `project_manager` (who holds `projects.view`) saw every project in the org's index/search/dashboard KPIs regardless of membership — fixed by removing that fallback. (2) `DashboardController` filtered tasks by project membership only, so a plain `employee` (never granted `tasks.tasks.view`) saw every teammate's task title on the dashboard even though `TaskPolicy::view()` would 403 them from opening it directly — fixed with a new shared `TaskPolicy::scopeVisibleToPerformer()` query scope, applied whenever the user lacks `tasks.tasks.view`. Also fixed 4 controllers' search filters (`Project`/`Device`/`Credential`/`Employee`) using Postgres-only `ilike` directly — new `App\Domain\Shared\Services\PortableSearch` makes them portable to SQLite too, which is why none of them had ever been exercised by a test before now. New tests added to `tests/Feature/Projects/ProjectTaskWebAccessTest.php`.
- **Files changed:** `app/Policies/{ProjectPolicy,TaskPolicy}.php`, `app/Http/Controllers/DashboardController.php`, `app/Http/Controllers/{Projects/ProjectController,Devices/DeviceController,Devices/CredentialController,Employees/EmployeeController}.php`, `app/Domain/Devices/Models/{Door,Zone}.php`, new `database/factories/{Door,Zone}Factory.php`, new `app/Domain/Shared/Services/PortableSearch.php`.
- **Contract/schema changes:** none. No migrations.
- **Tests/gates:** `php artisan test --compact` → 145 passed / 2 skipped (752 assertions; baseline was 142/2/686). `vendor/bin/phpstan analyse` → 0 errors (was 9). `vendor/bin/pint --dirty` → clean. `npm run types:check` → clean. Not yet re-run this pass: `npm run build`, connector tests, `git diff --check` (deferred to the Wave 1 exit gate).
- **Browser verification:** pending — no browser surface available this session, consistent with the audit's own stated limitation. All verification above is SQLite-based automated tests; no schema/RLS changed in this pass, so Postgres-specific re-verification isn't required for FIX-01/02 specifically but remains outstanding for the wave overall.
- **Next:** ADMIN-01 (replace the `admin@protect.ge` email-string `Gate::before` bypass with an explicit, auditable platform-admin grant), then TENANT-01, MONEY-01, ATT-01 to close out Wave 1.

### 2026-09-21 — User-authorized overnight implementation assignment

- Read [overnight goal and execution runbook](claude-overnight-goal.md), then maintain [persistent progress checkpoint](claude-overnight-progress.md).
- Claude may implement the linked backend and frontend tickets in small verified slices, continuing to the next eligible task without routine confirmation. Preserve existing work and check active file ownership.
- The runbook contains the ordered backlog, acceptance gates, tenant/database safety, read-only BioStar direction, mandatory single/bulk timesheet emailing, blocker handling and context-resume procedure.
- This entry assigns work; it does not claim the fixes have been implemented. Provider limits and unavailable external services cannot be overridden by the instruction.

### 2026-09-21 — Codex audit and implementation instructions for Claude Code

- **User-requested handoff:** read [platform audit](platform-audit-2026-09-21.md) and [implementation tickets](claude-platform-completion-2026-09-21.md) before continuing. They distinguish existing functionality, code-confirmed defects, and proposed additions, with file scopes and acceptance scenarios.
- **Architecture direction:** retain BioStar as device/card/access-policy manager; CRM imports events and owns attendance, timesheets, payroll and reports. Read-only enforcement is a requested next implementation step, not a change already made by this audit.
- **New explicit requirement:** email a single timesheet or a selected batch. The tickets cover recipient selection, PDF snapshots, per-recipient privacy, queues, send history, retry and realistic delivery status.
- **Verified in this audit:** Laravel tests 142 passed / 2 skipped (686 assertions); connector mock tests 13 passed; Vue typecheck, Pint and production build passed. PHPStan fails with 9 findings in Door, Zone and ProjectPolicy.
- **Priority defects:** inconsistent project/task visibility; email-based full-admin Gate; demo My Day; incomplete BioStar event pagination/employee mapping; attendance event-result and historical credential-assignment filtering; incomplete outbox processing; financial ownership/currency/concurrency checks; missing task photo viewing and Daily Journal pages.
- **Ownership for this handoff:** the user requests Claude Code to implement the scoped backend and frontend tickets in the linked instructions. Check current diffs and any actively owned files before edits. Codex's audit introduced documentation only, alongside generated build output and verification against disposable test storage; no business-code fixes were applied.
- **Verification limit:** the initial browser surface inventory returned no browsers/apps. Do not interpret this audit as a completed live browser or physical-reader acceptance run.

### Codex in progress

- Company tenancy foundation and Company CRUD UI are implemented and verified.
- Company tenancy foundation is complete. Device registry enrichment is now
  complete: additive production fields (name/vendor/identifier/IP/port/MAC,
  hardware/firmware, connection mode, timezone, metadata, enabled, last seen,
  last sync), searchable identity fields, compatibility-safe requests, and
  heartbeat/event/sync timestamp projections are wired.
- Next backend scope: Door / Zone and vendor-neutral
  `AccessControlProvider` abstraction, followed by queue-backed sync state.
- Project Manager dashboard is still placeholder-only and is the next product
  UX/backend integration target.

### Claude Code available scope

- Projects/Tasks UI and the Project Manager dashboard are now built (see the
  2026-09-17 entry below). Remaining Projects/Tasks UI not yet built: Kanban
  drag-to-transition (deliberately not wired to a real status mutation — see
  entry), calendar view, "My Day", WBS/membership/document management UI
  (backend Actions exist, no controller/UI yet), Gantt/dependency scheduling
  (P2 per spec).
- Still do not edit Devices, Companies, access-control migrations or
  connector files unless Codex explicitly reassigns a file here.

### Latest verified gates

- Backend: 108 passed, 2 skipped (Devices group: 24 passed, 153 assertions).
- PHPStan: 0 errors; Pint and TypeScript passed.
- Production Vite build passed.

### 2026-09-17 — Codex

- **Scope:** Devices production registry enrichment; additive migration
  `2026_09_17_180000_extend_devices_for_production_registry.php`, model/action/
  request/resource updates, Create/Edit/Show UI fields, search by name/serial/
  identifier, and last-seen/last-sync projections.
- **Contract:** Existing device requests remain valid; new identifiers are
  organization-scoped and nullable until connector discovery. Hard delete is
  still intentionally unavailable because device history is immutable.
- **Tests/gates:** Devices 24/24 passed (153 assertions), full suite 106/108
  passed with 2 skips; focused PHPStan 0 errors; Vue typecheck and production
  build passed. Whole-repo Pint still reports pre-existing Task resource
  formatting issues; the changed Devices actions were formatted and pass.
- **Next agent:** Claude Code may continue Projects/Tasks UX only. Codex owns
  Door/Zone schema and provider abstraction; do not edit Devices contracts
  without recording the change here first.

### 2026-09-17 — Codex (Door/Zone foundation)

- Added additive `zones` and `doors` schema with organization/site ownership,
  optional zone/device relationships, lock/REX/sensor JSON configuration,
  direction, enabled state, indexes, foreign keys and PostgreSQL RLS.
- Added `Zone` and `Door` Eloquent models. Device remains a separate entity;
  doors reference devices rather than being conflated with them.
- Devices regression suite remains green after the migration (24/24).

### 2026-09-17 — Codex (provider boundary)

- Added vendor-neutral `AccessControlProviderInterface`, retaining
  `DeviceAdapterInterface` as a backwards-compatible parent contract.
- Simulator and Suprema implementations now satisfy both contracts; the
  service provider aliases the new boundary to the configured adapter.
- Added deterministic `FakeAccessControlProvider` for hardware-free tests and
  local development. No Suprema/G-SDK dependency was introduced.
- Focused PHPStan and Devices tests pass (24/24).

### 2026-09-17 — Codex (Claude continuation recovery)

- Claude Code had stopped mid-edit after adding Project Show props. The
  controller was inspected and is internally consistent; Project Show now has
  working members, WBS/location and document sections with their existing
  backend routes/actions.
- TypeScript check and production build pass. Codex did not alter Tasks or
  Devices ownership.

### 2026-09-17 15:40 — Claude Code

- **Scope:** Projects/Tasks web UI (Index/Create/Show/Edit for both) + a
  real Project Manager dashboard, per the "Claude Code available scope"
  section above. Read `ProjectController`, `ProjectPolicy`, `TaskPolicy`,
  every `App\Domain\Tasks\Actions\*` class, and all related models/factories
  before writing anything, per protocol.
- **Files changed (new):**
  `app/Http/Controllers/Tasks/TaskController.php`,
  `app/Http/Controllers/DashboardController.php`,
  `app/Http/Requests/Tasks/*` (10 files),
  `app/Http/Resources/Tasks/{TaskResource,TaskDetailResource}.php`,
  `app/Providers/Projects/ProjectsModuleServiceProvider.php`,
  `app/Providers/Tasks/TasksModuleServiceProvider.php`,
  `database/seeders/modules/TasksPermissionsSeeder.php`,
  `config/modules/projects-nav.php`, `routes/modules/web-projects.php`,
  `resources/js/pages/Projects/{Index,Create,Show,Edit}.vue`,
  `resources/js/pages/Tasks/{Index,Create,Show,Edit}.vue`,
  `tests/Feature/Projects/ProjectTaskWebAccessTest.php`,
  `docs/handoff-nav-empty-issue.md` (an earlier live-bug diagnosis; safe to
  delete once you've read it, or tell me and I will).
- **Files changed (existing, small/additive edits — flagging per protocol
  rule 3):**
  - `app/Providers/AppServiceProvider.php`: added
    `JsonResource::withoutWrapping()`. **Real bug, not a style choice**: any
    single Resource passed directly as an Inertia prop (e.g.
    `ProjectController@show`'s `'project' => new ProjectDetailResource(...)`)
    was being wrapped in `{"data": ...}` by Laravel's default, so
    `project.code` etc. were `undefined` in the browser. This is the
    standard, official Laravel recommendation for SPA/Inertia apps. Verified
    via the new Projects/Tasks test — it failed with exactly this symptom
    before the fix, passed after. If anything elsewhere in the app relied on
    the `data` envelope shape, it will need updating — I found none in the
    current test suite (all 108 tests pass) but flagging since this is
    app-wide.
  - `app/Domain/Tasks/Actions/CreateTask.php`: added an explicit
    `'status' => 'draft'` to the `Task::create([...])` call. Real bug: it
    read `$task->status` immediately after `create()` without ever setting
    it, and Eloquent does not re-fetch a DB-default column into the
    in-memory model post-insert, so `TaskStatusEventRecorder::record()` got
    `null` and threw a `TypeError` on every task creation. Caught by the new
    test, not by any existing one (no prior Tasks controller/test existed).
  - `app/Domain/Tasks/Actions/UploadTaskAttachment.php`: fixed
    `config('modules.tasks.attachments')` → `config('tasks.attachments')`
    (typo — `config/modules/tasks.php` merges under the `tasks` key per
    `TasksModuleServiceProvider`, matching every other module's convention;
    the old key never resolved).
  - `app/Domain/Tasks/Models/{Task,TaskSubmission,TaskAcceptance,TaskStatusEvent}.php`:
    added explicit `@property Carbon|null $due_at` /
    `$submitted_at`/`$accepted_at`/`$occurred_at` docblocks. PHPStan/Larastan
    doesn't reliably infer datetime casts declared via the Laravel 11+
    method-style `casts()` (vs. the older `$casts` property) — this is the
    same pattern already used in `CredentialAssignment.php`. No behavior
    change, fixes 9 real PHPStan errors.
  - `app/Http/Controllers/Projects/ProjectController.php`: added
    `changeStatus()` action + `projects.status` route. The Policy
    (`changeStatus`), Request (`TransitionProjectStatusRequest`), and Action
    (`TransitionProjectStatusAction`) all already existed with no controller
    method wired to them — `Show.vue` needed it for the status-change form.
  - `app/Providers/{Projects,Tasks}ModuleServiceProvider` (new, see above):
    **`ProjectPolicy` and `TaskPolicy` were not registered anywhere** — no
    module provider existed for either. Laravel's policy auto-discovery does
    not reliably resolve policies for models nested under
    `app/Domain/<Module>/Models` in this codebase (same reason every other
    module has an explicit `protected $policies` array). Without this,
    every `$this->authorize()` call in `ProjectController` and the new
    `TaskController` would have thrown. Verified via
    `Gate::getPolicyFor(...)` resolving correctly post-fix.
  - `database/seeders/AggregatingPermissionsSeeder.php` +
    `TasksPermissionsSeeder.php` (new): **no Tasks permissions
    (`tasks.tasks.*`) existed in the `permissions` table at all** — no
    seeder had ever created them. Added following `ProjectsPermissionsSeeder`'s
    exact pattern and registered it in the aggregator's `ORDER` list. Also
    re-ran `php artisan db:seed --class=AggregatingPermissionsSeeder --force`
    against the live dev DB (idempotent, `firstOrCreate`-based) so this
    doesn't only take effect for fresh test databases.
  - `tests/Feature/ExampleTest.php`: updated to assert the new real
    root-redirect behavior (`/` → login/dashboard) instead of the old
    Laravel-starter-kit "Welcome" 200 response — that page was removed in an
    earlier session (see the conversation history / `routes/modules/web-shared.php`),
    this was the one leftover failing test.
- **Contract/schema changes:** none to the database schema. New routes only
  (`projects.*`, `projects.tasks.*`, `dashboard` now backed by a real
  controller instead of a bare `Route::inertia`).
- **Deliberate product decision, not fully spec-complete:** the dashboard's
  Kanban board renders real task data but its drag-and-drop `move` event is
  intentionally a no-op (`@move="() => {}"`) — real task status transitions
  each have their own preconditions (performer-only submit, reason-required
  block/cancel, etc.) that don't map to a generic drag-to-column mutation.
  Card actions link to the task's real page instead. KPI tiles only show
  Projects/Tasks numbers I can actually compute — no placeholder
  attendance/tool figures.
- **Tests/gates:** `php artisan test` 108 passed / 2 skipped (0 failed);
  `vendor/bin/phpstan analyse` (whole app) 0 errors; `vendor/bin/pint --dirty`
  clean; `npm run types:check` clean; `npm run build` passed.
- **Next agent:** Projects/Tasks now have a real, tested golden path
  (create project → create task → assign → start → submit → accept →
  completed, verified end-to-end in
  `tests/Feature/Projects/ProjectTaskWebAccessTest.php`). Still open on the
  Claude Code side: WBS/membership/document management UI, Kanban/calendar/
  My Day views, task comments' `mentions` UI (backend accepts an array,
  no picker built). `docs/handoff-nav-empty-issue.md` documents an earlier
  live-session bug (empty sidebar nav) that turned out to be caused by
  missing `devices.*` permissions never being seeded — same root-cause
  category as the missing Tasks permissions found here; worth a project-wide
  check for any other module whose `*PermissionsSeeder` was written but never
  added to `AggregatingPermissionsSeeder::ORDER` or re-run against the dev DB.

### 2026-09-17 evening — Claude Code

- **Scope:** Two live-session bug reports (Companies creation, Employees
  positions) plus the Projects/Tasks UI continuation Codex's prompt
  requested. Read `docs/agent-handoff.md`, `docs/handoff-nav-empty-issue.md`,
  the Projects/Tasks controllers/resources/pages, and the Companies/
  Employees domain before touching anything, per protocol.
- **Bug 1 — company creation (`app/Domain/Companies/Actions/CreateCompanyAction.php`):**
  two real bugs, confirmed via `storage/logs/laravel.log`'s record of the
  user's actual failed attempts against the real Postgres dev DB. (a) The
  manual duplicate-code pre-check did `where('code', $data['code'])`, which
  Laravel turns into `whereNull('code')` when code is blank — so adding any
  *second* company with an empty code falsely failed once one company in the
  org already had a blank code. (b) The same check is a non-atomic
  check-then-insert; a race (fast retry/double-click) let the DB's real
  `unique(organization_id, code)` constraint reject the insert as an
  **uncaught 500**, which knocks Inertia out of the SPA entirely (exactly the
  "window disappears" symptom reported) instead of showing a validation
  error. Fixed both: skip the pre-check when code is null, and catch
  `UniqueConstraintViolationException` around the transaction and convert it
  to the existing friendly `ValidationException`. Regression tests added in
  `tests/Feature/Companies/CompanyWebAccessTest.php`. Verified against the
  real dev DB directly (not just sqlite tests) that companies are created
  and immediately visible via the exact index query — 8 companies now exist
  for the reporting org, all real.
- **Bug 2 — employee "positions" (new feature, at the user's explicit
  direction after a scoping question):** `employees.position` was free text
  with no predefined list, so managers couldn't reliably filter "who is in
  position X". Added a small `Position` module: migration
  `2026_09_17_200000_create_positions_and_add_position_id_to_employees.php`
  (additive `positions` table + RLS + nullable `employees.position_id` FK;
  the legacy free-text `position` column is untouched for old records),
  `App\Domain\Employees\Models\Position`, `PositionPolicy` (reuses
  `employees.employees.*` permissions — no new permission pair), a small
  `PositionController` (list + inline create/edit, no destroy — matches the
  Companies/Devices "disable, don't delete a referenced record" precedent),
  routes/nav under the Employees module. `Employee::jobPosition()` is
  deliberately NOT named `position()` — that name collides with the existing
  `position` database column and would be silently unreachable via magic
  property access (Eloquent always resolves a same-named attribute over a
  relation). Wired into `EmployeeController` (index filters by
  `position_id`/`supervisor_employee_id` — the latter was also missing and
  is part of the same "filter by manager" ask), `StoreEmployeeRequest`/
  `UpdateEmployeeRequest`/`*EmployeeAction`, `EmployeeResource`, and
  `EmployeeForm.vue` (free-text input replaced with a `<select>` fed by the
  new list). Tests added in `tests/Feature/Employees/EmployeeAccessTest.php`.
- **New feature — `/me/profile` ("ჩემი პროფილი"):** at the user's request for
  an employee self-service view of hours/salary/attendance. Scoped down
  after flagging that Attendance/Payroll (REQ-ATT/REQ-TSH/REQ-PAY) are all
  still `not-started` in `docs/implementation-plan.md` — the user chose the
  minimal option. `App\Http\Controllers\MyProfileController` derives the
  Employee from `$request->user()->id` only (never a route parameter, so
  there is no IDOR surface by construction) and is intentionally gated by
  nothing beyond `auth` — seeing your own data doesn't need a permission
  grant. It shows real profile fields and real raw device check-in/out
  events (`RawAccessEvent`, already ingested by the Devices module) with an
  honest "not available yet" empty state for worked-hours/salary rather than
  fabricating numbers. New nav entry in `shared-nav.php` (permission `null`
  — visible to everyone), tests in `tests/Feature/Shared/MyProfileTest.php`.
  Updated `tests/Feature/Shared/NavigationServiceTest.php`'s now-two-item
  "მიმოხილვა" group assertion accordingly.
- **Projects/Tasks UI continuation (Codex's prompt scope):** Project
  Index/Show now show and filter by `company` (controller + `ProjectResource`/
  `ProjectDetailResource` gained a `company` relation/prop — was previously
  loaded nowhere despite the column existing). Task Index gained a
  `priority` filter (controller previously only supported `status`/
  `accountable_owner_employee_id`) plus a clear-filters affordance on both
  lists. `Projects/Show.vue` was restructured from one long mixed-language
  (English labels like "Project members"/"Work breakdown" inside an
  otherwise Georgian page) scroll into real tabs — Overview/Tasks/Members/
  WBS/Documents/Activity — all in Georgian, with a real (not fabricated)
  task-status-count summary (`taskStats` prop, new) and destructive-action
  confirmations on member/location/document removal that had no UI before.
  Activity tab is an honest empty state (no activity feed exists yet — not
  fabricated). Did not build a Kanban duplicate inside Show — it links to
  the existing full `/tasks` page instead, to avoid two divergent
  implementations of the same status-transition rules.
- **Contract/schema changes:** additive only — `positions` table,
  `employees.position_id` (nullable FK), no destructive changes anywhere.
  New routes: `positions.index/store/update`, `me.profile`. New Inertia
  props: `Projects/Index`'s `companies`/`filters.company_id`, `Projects/Show`'s
  `taskStats`, `Tasks/Index`'s `employees`/`filters.priority`,
  `Employees/Index`'s `positions`/`supervisors`/two new filter keys,
  `Employees/Create|Edit`'s `positions`.
- **Tests/gates:** `php artisan test` 115 passed / 2 skipped (0 failed);
  `vendor/bin/phpstan analyse` (whole app) 0 errors; `vendor/bin/pint --dirty`
  clean; `npm run types:check` clean; `npm run build` passed. Migration run
  against the real dev DB (`php artisan migrate`), not just the sqlite test
  DB.
- **Next agent:** Still open from the previous entry: Kanban drag-to-transition
  (deliberate no-op), calendar view, comments' `mentions` picker UI. New:
  Attendance/Payroll modules are the real blocker on `/me/profile` ever
  showing real hours/days/salary — when that module agent runs, it should
  fill in `MyProfileController`'s two empty-state sections with real
  computed data rather than adding a second, competing "my hours" surface.
  `docs/handoff-nav-empty-issue.md`'s bug is unrelated to anything found
  here (that was a permission-cache/duplicate-account issue on a demo
  account, confirmed not reproducible for the real reporting user, who has
  correct `owner` permissions) — safe to delete once someone confirms the
  live nav is populated after a hard refresh.

## Handoff entry format

```text
### YYYY-MM-DD HH:MM — Agent
- Scope:
- Files changed:
- Contract/schema changes:
- Tests/gates:
- Next agent:
```
